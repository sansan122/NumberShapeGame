# 数与形 · NumberShapeGame

一个把**数学知识点**变成卡牌机制的肉鸽卡牌游戏原型。

---

## 一、怎么运行（最重要）

### 给自己玩 / 开发

在 `C:\Users\sanji\Desktop\游戏制作尝试\` 里，**双击 `0_START_HERE.bat`**，
然后按提示输入数字回车：

```
[1] 公理塔地图       ← 主游戏，爬塔选路线
[2] 卡牌战斗         ← 战斗演示
[3] 重新生成地图     ← 固定种子，每次同一张
[4] 随机生成地图     ← 每次都是新地图
[5] 我的第一个窗口   ← 最早那个红方块窗口
[0] 退出
```

也可以直接双击对应的单独脚本：

| 想做什么 | 双击这个 |
|---|---|
| 玩地图（主游戏） | `3_run_game.bat` |
| 只看地图 | `2_run_map.bat` |
| 玩卡牌战斗 | `1_run_card.bat` |
| 重新生成地图（固定） | `map_tools\2_gen_map.bat` |
| **随机生成地图** | `map_tools\3_gen_map_random.bat` |

> 这些 `.bat` 现在会**自己找 Python**（项目自带 venv → PATH → `py` → 常见安装路径），
> 不再写死本机路径，所以拷到别的机器上也能用。

### 给别人玩

跑一次打包：

```
<python> 打包.py
```

产出的 `release/数与形-公理塔.zip`（约 25 MB）**直接发出去**。
对方双击里面的 `NumbersAndForms.exe` 就能玩，**什么都不用装**。
详见第八章。

### 打开游戏后你会看到什么

现在启动主游戏，不是直接扔进地图，而是**先出一个开场界面**：

```
主菜单  →  选角色  →  载入中  →  正式游戏
          （新游戏）
主菜单  →  直接回到上次的存档            （继续游戏）
```

**主菜单**三个按钮：`新的爬塔` / `继续游戏`（有存档时才亮）/ `离开`。

**选角色**界面里横排三张卡片，每张卡上有：

- 一块**主题色方框**——这是**头像占位**，美术图后面再换（详见第三章末）
- 角色名、称号、大号数学符号（`∑` `△` `χ`）
- 生命条 + 生命 / 金币数值
- 一段定位说明 + 底部的**被动技能**文案

操作：点卡片一下是**选中**（边框变金），再点一下**出发**。
键盘也行：`←` `→` 或 `A` `D` 切换，`回车` / `空格` 出发，`ESC` 退回主菜单。

选完会有一个约 0.7 秒的**载入画面**（显示角色符号、「XX 踏入了公理塔」、
当前楼层名、「正在生成地图·」），按任意键可以跳过 ——
这一瞬间程序正在**现场生成这一局的地图**，所以每次进来都不一样。

> ⚠️ **不要修改 `.bat` 文件里的中文，也不要用记事本另存为 UTF-8。**
> 这几个 `.bat` 是特意做成"纯英文内容 + GBK 编码"的。
> 原因见下面「避坑记录」。

---

## 二、地图怎么玩

这是参考《杀戮尖塔》的爬塔地图：**起点在最下面，一路往上爬到层主**。

打开 `3_run_game.bat` 后是一个 1280×720 的窗口：

| 操作 | 效果 |
|---|---|
| **鼠标滚轮** / `↑` `↓` | 上下滚动视角（镜头跟随） |
| **点击高亮的节点** | 移动过去，进入那个节点 |
| **点击远处的节点** | 不移动，而是高亮出「从当前位置到它」的完整路径 |
| **点左侧「遗物 N 件」** | 打开遗物图鉴（自己有哪些、各自什么效果） |
| `R` / **点左侧「查找遗物」** | 在地图上把**会出遗物**的节点标出来 |
| `D` | **查看自己的牌组**（面板可滚动，详见第六章） |
| `S` | **存档**（弹出三个存档槽选一个，详见第七章） |
| `L` | **读档**（弹出三个存档槽选一个） |
| `M` | **回主菜单**（地图 / 节点 / 战斗都能按；死亡画面上也有提示） |
| `F1` | **音效开 / 关**（开关会记住，下次打开还是这个状态） |
| `F2` | **背景音乐开 / 关** |
| `ESC` | 退出（图鉴开着时，这一下只关图鉴，不会把游戏退掉） |

**滚轮方向**：往上滚 = **往塔的上方看**（能看到更高的楼层）。
这条约定收口在 `MapScene.scroll_up()` / `scroll_down()` 两个方法里 ——
不要在事件层直接调 `scroll()` 传正负数：`cam_y` 变大其实是**往下看**
（画面 y = `HEIGHT/2 + (世界y - cam_y)`，cam_y 变大内容就上移），
很容易写反。踩过一次，详见第十一章第 21 条。

**哪些节点能走？** 只有**下一行**的节点，它们会带一圈**蓝色呼吸光晕**。
其余节点是暗的，点不了，点了只会给你看路径。

**地图每次都不一样。** 每开一局都是现场生成的新布局
（详见第九章「游戏的地图是运行时现场生成的」）。

### 节点图标对照

图标用的是**数学符号**，和《数与形》的主题呼应：

| 图标 | 类型 | 说明 |
|---|---|---|
| `×` | 普通战斗 | 常规敌人，掉落卡牌 |
| `◆` | 精英战斗 | 阶段化敌人，掉落遗物 |
| `=` | 休整点 | 回复生命或强化一张卡 |
| `√` | 商店 | 用金币买卡牌、遗物，或移除卡牌 |
| `?` | 事件 | 三选一抉择，带世界观文本 |
| `∑` | 宝箱 | 直接获得一件遗物 |
| `★` | 层主 | 本层最终战 |

### 找遗物：图鉴 + 地图标记

遗物是这一局最值钱的东西，但以前地图左侧只写「遗物 3 件」一个数字 ——
玩家**没地方查**这几件到底干什么用。遗物效果是**按名字结算**、
代码散在 `battle_scene` / `node_scenes` 好几处（见 `player.RELIC_POOL`
上面那行注释），不查就只能靠猜：「换元法」是省一张牌还是加伤害？

现在有两件工具（都在地图界面上）：

**① 遗物图鉴**（点左侧「遗物 N 件」，或任意时候按面板右上角提示）
居中的一块面板，把遗物池**一次列全**：

| 样子 | 含义 |
|---|---|
| 紫色边框 + 「已获得」角标 | 这件已经在你身上了，效果文字是实色 |
| 灰边框 + 「未获得」角标 | 还没拿到，效果文字变浅 |

面板底部写着**来源**（宝箱：必得 1 件 / 精英：战后 1 件 / 层主：战后 1 件 /
商店：柜上有 1 件 / 事件：三选一里有 1 条），所以「下一件去哪儿找」
本身就是这个面板回答的一半问题。
关掉的方式有三种：点「关闭（ESC）」、按 `ESC`、点面板外面 ——
只留一个关闭按钮的话，小朋友会去点旁边空白处然后以为界面卡死了。

**② 查找遗物**（按 `R`，或点左侧「查找遗物」按钮）
打开之后：

- 屏幕上**会出遗物的节点加一圈紫环**，下面挂一个标签写明出处
  （「遗物 · 宝箱 必得 1 件」）；
- **不出遗物的节点压暗**，让来源节点跳出来；
- 顶部一条横幅写着**本层一共几处、分别是什么**
  （「本层 10 处：宝箱 1　精英 5　层主 1　商店 1　事件 2」）。

没有查找模式时，来源节点右上角也常驻一个小小的紫色「遗」角标 ——
路线规划时可以一眼看出「走哪边能顺路拿遗物」。

> 压暗只是**换颜色**，没有盖半透明纱：盖纱会把「可到达」的呼吸高亮
> 一起糊掉，而「这一行我能点哪儿」比找遗物更要紧。
> 站着的那个节点也永远保持原色，不然玩家会一时找不到自己。

**「哪里出遗物」只有一张表。** `map_scene.RELIC_SOURCE` 是显示用的，
真正发遗物的另有其处（`ENEMY_KINDS["elite"]["relic"]`、宝箱面板、
商店货架、事件选项）。这两边会漂移，所以 `tmp/verify_relic_find.py` 里
有一条断言**去真的读那几个源头**再和表对账 —— 地图上说错「哪里有遗物」，
比干脆不标更糟。

### 路线代价（本作的差异化机制）

连线上如果有个**金色小圆圈**，说明这条边带「路线代价」——走上去要付出代价，
但代价的名字全部来自数学概念，让惩罚本身也变成一次记忆强化：

| 代价 | 效果 | 数学出处 |
|---|---|---|
| **取近似** | 跳过 2 个节点，但失去 8 点最大生命 | 四舍五入求省事，必然丢精度 |
| **开区间** | 下一个节点必为精英，但战后额外获得 1 次强化 | 开区间端点取不到 |
| **未完待证** | 目标节点类型不可见 | 条件未证，结论未知 |
| **负债增量** | 获得 1 件随机遗物，但本层所有敌人血量 +15% | 借来的量要连本带利还 |

把鼠标悬停在带代号的连线上，可以看到这个代价的完整说明和数学出处。

> **「本层」是真的本层**：`+15%` 在换层时归位（`MapScene.load_floor`）。
> 以前没有这一步，代价选过一次就永久跟着玩家爬到塔顶，而它的收益
> （1 件遗物）只兑现一次 —— 越往后越是纯亏。

---

## 三、开场界面与三名角色

### 三个角色的数值对比

| | **演算者** | **构形师** | **解方程者** |
|---|---|---|---|
| 称号 | 数之刃 | 形之壁 | 未知之追 |
| 符号 | `∑` | `△` | `χ` |
| 主题色 | 蓝 `(24,95,165)` | 绿 `(59,109,17)` | 紫 `(150,60,150)` |
| 生命上限 | 80 | **92** | 72 |
| 起始金币 | 60 | 50 | **75** |
| 定位 | 数字卡强化 | 格挡与反伤 | 抽牌与连击 |
| 被动（已生效） | 【直感】每回合打出的第一张数字卡伤害 +1 | 【承形】回合结束时若还有未用完的格挡，下回合 +2 格挡 | 【代入】每回合抽牌阶段多抽 1 张 |

**三个被动都真的接进战斗了**，不再是卡面上的文案。
结算全部写在 `battle_scene.py` 里（按 `char_id` 判断，不额外占存档字段），
而且战斗日志会写明是哪条被动生效、生效了多少 —— 方便对账：

```
「直感」生效：本回合首张数字卡 +1 伤害
「承形」生效：+2 格挡
```

> 几个容易做错的细节，实现时都踩过：
> - **【直感】判定在 `resolve_card` 而不是 `submit_quiz`** ——
>   算对才叫「打出」。放在答题环节的话，算错一次就把直感白嫖掉了。
> - **【直感】只认数字卡**：先打一张图形卡不会把这一次用掉。
> - **【承形】看的是「点结束回合那一刻」的格挡**，不是敌人打完之后剩多少 ——
>   敌人把格挡打光，也照样给下回合的 2 点。
> - **【代入】的第一回合也算**：起手就是 6 张，不是从第二回合才开始。

### 每个角色有独立的起始牌组（数字牌 0~9 + 自己的形卡）

不是三个人共用一套牌。**三份牌组都带齐数字牌 0~9（十张）**，
区别在剩下的五张「形」卡：

| 角色 | 张数 | 牌组特点 | 它带的形卡 |
|---|---|---|---|
| 演算者 | 15 | 牌组最精简，靠【直感】白赚伤害 | 三角盾 ×2、方阵、镜像、归零 |
| 构形师 | 17 | 格挡最厚（伤害本来就低，靠磨） | 三角盾 ×3、方阵 ×2、反证、归零 |
| 解方程者 | 16 | 过牌快，手牌多 | 三角盾、镜像 ×2、换元、归零 |

> **三个人起手都没有「平方」** —— 它是打穿第一层层主之后的战利品（见下面
> 「层主」与「战利品」两节）。第一层只能靠 0~9 的加法慢慢磨，
> 拿到平方之后 9 号牌才有 81 点的爆发。**这个前后差就是全篇的难度曲线。**

### 数据定义在哪儿

三个角色都写在 `player.py` 顶部的 `CHARACTERS` 列表里，一条就是一个字典：

```python
{
    "id": "calculator",       # 存档里记的就是这个 id
    "name": "演算者",
    "title": "数之刃",
    "icon": "∑",              # 头像占位用的大符号
    "color": (24, 95, 165),   # 主题色：头像框、卡片高亮都用它
    "hp": 80,
    "gold": 60,
    "theme": "数字卡强化",
    "desc": "……",             # 卡片上那段定位说明
    "deck": [...],            # 起始牌组；为 None 时用默认 STARTER_DECK
    "trait": "……",            # 被动技能文案
}
```

想加第四个角色，往 `CHARACTERS` 里再 append 一条就行，
选角色界面会自动多出一张卡（卡片是横向平均排布的，暂时按 3 张算，
加到 4 张需要顺手改一下 `CharSelectScene` 里的 `cw` / `gap`）。

### 美术资源（立绘与动作动画）

三个角色都有真实的像素立绘 + 三组动作（待机 / 攻击 / 受击）。
资源放在 `assets/chars/{角色id}/`：

```
assets/chars/calculator/
├── full.png            静态立绘（选角卡片用）
├── idle_00…07.png      待机循环（8 帧 × 100ms）
├── attack_00…15.png    攻击（播完自动回待机）
└── hit_00…07.png       受击（播完自动回待机）
```

**这些 PNG 是自动生成的**：源素材是 `人物(1)/` 里的 Aseprite 工程和 GIF
（源文件不进 git），跑一次 `tmp/prepare_char_art.py` 就拆成上面的帧序列。
为什么要在开发期先转好：pygame 不能读 GIF 动画，而打包后的 exe 又不带 PIL，
所以"拆帧"这一步只能放在开发机做，产物进仓库。

预处理的两个要点：
- **全局联合 bbox**：同一角色的静态图 + 三个动作的**所有**帧用同一个裁剪框，
  这样帧与帧、动作与动作之间坐标系一致，切换动作时角色不会跳位置
- 统一缩放到高 320px，运行时再按需缩放

**加载与播放在 `char_art.py`**：

| 情况 | 行为 |
|---|---|
| 素材缺失 / 文件损坏 | `has_art=False`，调用方退回「符号 + 主题色圆」占位，**不会崩** |
| 待机 | 循环播放 |
| 攻击 / 受击 | 播完自动回待机（连着挨打时不会重头打断） |
| 缩放 | 结果按「帧身份」缓存，不掉帧 |

三处都在用它：战斗立绘（`battle_scene`）、选角卡片（`ui_scenes`）、
地图上的像素小人（`map_scene`）。

**想加第四个角色**：素材丢进 `人物(1)/` → 在 `tmp/prepare_char_art.py`
的 `CHAR_MAP` 加一行 → 跑一次脚本 → 在 `player.CHARACTERS` 加一条数据、
`char_art._DIRS` 加一行目录映射。三处界面会自动生效。

### 角色怎么跟着存档走

存档里多了一个 `char_id` 字段（见第七章）。读档时：

- 有 `char_id` → 按它还原角色
- 老存档没有这个字段 → 兜底用 `"calculator"`（演算者），**不会崩**

---

## 四、点击节点之后会发生什么

走到节点上，会按类型自动进入对应的内容：

| 节点 | 进去之后 |
|---|---|
| `×` 普通战斗 | 打「几何魔像」（第 1 层 50 血，胜了给 28 金币） |
| `◆` 精英战斗 | 打「方程组·三元」（78 血，给 55 金币） |
| `★` 层主 | 打**本层自己的层主**（名字和血量写在 `tower.yaml`，见下） |
| `=` 休整点 | 二选一：**回复生命**（30% 上限）或**强化一张卡**（数值 +3） |
| `√` 商店 | 3 张随机卡 + 1 件遗物。另有「移除一张卡」的服务（见下） |
| `?` 事件 | 随机抽一个事件，每个事件有 3 个选项，**选择会真的改变玩家状态** |
| `∑` 宝箱 | 点开箱子，**直接获得一件随机遗物** |

### 三层的层主与难度递增

`tower.yaml` 每层配了三样东西：

| 层 | 层主 | 层主血量 | `enemy_scale` |
|---|---|---|---|
| 1 塔基·数之庭 | 正方体·三阶（**平方主题**） | 130 | 1.00（不变） |
| 2 塔身·形之廊 | 方程组·三元 | 200 | 1.45 |
| 3 塔冠·方程之顶 | 不可解之影 | 270 | 1.90 |

`enemy_scale` 是**本层所有敌人**的综合强度倍率：

- **生命按满额放大**（第 2 层杂兵 72 血、第 3 层 95 血）—— 打得更久＝挨更多打，
  这是难度的正常来源
- **攻击只吃一半增幅**（`battle_scene.ENEMY_ATK_GENTLE = 0.5`）：
  攻击区间从 7~12 → 10~17 → 约 10~17 再上一档。玩家没有任何稳定的回血手段，
  攻击跟着满额涨的话第三层一个杂兵三回合就能削掉半管血，那不是难度是劝退
- 这和路线代价「负债增量」的 +15% 是**两回事，两者相乘**：
  负债增量是玩家自己选的代价，`enemy_scale` 是塔本身的难度
- **层主血量不吃 `enemy_scale`** —— 每层的层主血量单独配。第一层还没有「平方」，
  用同一个倍率会把它压到打不过

#### 第一层层主：正方体·三阶（平方主题）

它是全篇唯一一个**教你怎么用「平方」的**敌人 —— 在你拿到那张牌之前。

出招是固定脚本（不掷骰子），每两回合一轮：

```
蓄力 4   →   平方 4²=16   →   蓄力 4   →   平方 4²=16   →   …
```

- **蓄力回合完全不还手**，这是留给你「这回合放心全力输出」的窗口
- 下回合那一下的意图框会**明确写出 16**（不是写「蓄力中」）——
  玩家的唯一解题思路就是「看见 16，这回合攒够格挡去接」。写模糊了这个决策就不存在
- 打赢它 → 战利品面板第一屏就是「**新卡到手：平方**」，
  而你刚刚就是被 n² 打过两下的，不用任何文字教程就知道这牌怎么用

它的形象也用平方说话：`art_shapes._enemy_square_boss` 画的是三个正方形
**共用同一条底边**，边长 1 : 2 : 3（= 面积 1 : 4 : 9）。

### 休整点

进去是两个大按钮：

- **回复生命** — 回满 30% 最大生命（80 上限就是回 24 点），点完立刻离开
- **强化一张卡** — 进入牌库网格，点哪张就强化哪张（伤害/格挡 +3，抽牌 +1），名字会带个 `+`
  - 左下角「返回」可以退回菜单改主意

### 商店

四件商品一字排开，价格按卡牌费用算（`40 + 费用 × 20`）：

- 买的卡**直接进你的牌库**，本局后续战斗都能抽到
- 遗物买下**立即生效**
- 「**移除一张卡**」服务 —— 两条规矩，缺一条删牌就会变成无脑最优解：
  1. **一家店只能删一张**，删完按钮变成「这家店删过了」
  2. **每删掉一张，下次就更贵**：60 → 90 → 120 → 150 …（`player.REMOVAL_BASE`
     + 档位 × `REMOVAL_STEP`，最低 1 档）

  价格由「已经删掉几张」推出来（`Player.removals_done`），不是存档里的独立字段 ——
  这样调定价对老存档立刻生效，不用迁移数据。删完的提示会直接告诉你下次多少钱，
  不然下一家店的价格会显得莫名其妙。
- **平方卡解锁之前，商店不进它的货**（`shop_pool()`）——
  否则第一层就能买到，「打完层主才拿到」这条线等于白设
- 金币不够会提示「还差多少」，不会误扣

### 事件

目前有 3 个事件，每个 3 个选项，都是**真实生效**的：

| 事件 | 选项 |
|---|---|
| **断裂的等式** | 补上缺口（-10 生命，得 1 件遗物）/ 绕过去（无事发生）/ 铭记它（最大生命 +8） |
| **未完成的证明** | 替它写完（+40 金币，最大生命 -6）/ 撕掉它（移除牌库一张卡）/ 折起收好（+25 金币） |
| **沉默的演算者** | 与它交换（-20 金币，最大生命 +10）/ 点头走过（+15 金币）/ … |

选完再点一下画面任意处即离开。

### 遗物一览

| 遗物 | 效果 |
|---|---|
| **勾股定理** | 战斗内**首次**打出图形卡，额外 +3 格挡 |
| **质数筛** | 战斗开始时，从牌库移除 1 张最弱的卡 |
| **换元法** | 每回合**第一次算错不消耗卡牌**（卡留着） |
| **对数尺** | 每回合多抽 1 张牌 |
| **约等号** | 所有伤害 +1 |
| **公理石** | 最大生命 +12 |

**六个遗物的效果都真的接上了**，而且都能指出具体在哪一行结算
（原来「公理石」和「质数筛」只是躺在遗物池里，代码里一处都没实现）：

| 遗物 | 结算位置 |
|---|---|
| 勾股定理 / 换元法 / 对数尺 / 约等号 | `battle_scene.py`，按名字判断 |
| 质数筛 | `BattleScene.__init__`，**洗牌之前**筛掉最弱的一张 |
| 公理石 | `Player.add_relic()` —— 拿到的那一刻上限和当前生命一起 +12 |

> - 质数筛必须赶在 `random.shuffle` / 抽起手牌**之前**动手，
>   否则筛掉的可能是已经抓在手上的那张，等于什么都没筛。
> - 发遗物统一走 `roll_unowned_relic()`：只抽玩家还没有的，六件集齐就折现 40 金币。
>   原来宝箱 / 商店 / 事件三处各写了一个 `random.choice(RELIC_POOL)`，
>   同一个遗物能发两次 —— 名字进了列表，效果却按名字只算一份，等于白给一件。

**游戏里怎么查这些效果**：地图上点左侧的「遗物 N 件」打开**遗物图鉴**，
拿到的高亮、没拿到的灰着，底部还写着各自的来源；按 `R` 则可以在图上
把出遗物的节点全找出来。详见第二章「找遗物：图鉴 + 地图标记」。

---

## 五、卡牌战斗怎么玩

打开后是一个 1280×720 的浅色窗口：

1. **点手牌选一张卡**（卡片会浮起变黄）
2. **再点一下空白处**＝决定打出这张卡
3. 这时会弹出一道题，等式右边是一条**下划线**（填空线）
4. **直接敲数字键**，输入的数字会实时显示在下划线上，光标跟随闪烁
   - 按 `退格` 删掉最后一位
   - 按 `回车` 提交（或点「提交」按钮）
5. 点右下角 **「结束回合」** 轮到敌人行动

判定结果：
- ✅ **算对了**：卡牌效果生效
- ❌ **算错了**：这张卡**直接进弃牌堆**，白白浪费

**核心设计**：想打牌，先算对。数学不是外挂的答题小游戏，而是出牌的**前置门槛**。

### 四种题型（出牌方式决定题目）

| 怎么出牌 | 出什么题 | 答对之后 |
|---|---|---|
| 数字卡**单独**打出 | 算术题（难度看卡上的数字，见下） | 这张卡生效 |
| 图形卡**单独**打出 | **认图形名称**（四选一） | 这张卡生效 |
| 数字卡 + 图形卡**组合** | **算图形面积** | **两张卡一起生效**，另多抽 1 张牌 |
| 数字卡 + 平方卡**组合** | **算这个数的平方** | 伤害＝**数字的平方**（数字 9 → 81） |

**组合怎么操作**：点一张数字卡（卡会浮起，底部提示「再点图形卡＝算面积，
点「平方」＝算 9²」），再点配得上的另一张 —— 直接进对应的题；反过来先点
图形 / 平方卡也一样。

**组合的费用 = 两张卡费用之和 − 1**（组合优惠，最低 1 点）：
组合要花两张卡、答一道更难的题、还只占一次出牌机会，不打折没人肯用。
能量不够时**不出题、也不扣能量**，牌和能量都留着。
（所以「单张卡贵不贵」和「组合贵不贵」是两件事：选牌时不查能量，
真出牌那一刻才查 —— 见避坑第 32 条。）

#### 数字牌 0~9：数字越大，题越难，打得越疼

牌组里的数字牌就是 **0~9 十张**，牌面上的数字就是这张牌本身
（伤害＝数字、图案也是那个数字）。题目难度跟着数字走：

| 数字 | 题目 | 卡面标签 |
|---|---|---|
| 0 | 一位数加法（和 ≤ 5） | 最简 |
| 1~2 | 10 以内加减 | 加减 |
| 3~4 | 20 以内加减 + 一位数乘法 | 含乘 |
| 5~6 | 两位数加减、乘、整除的除法 | 四则 |
| 7~8 | 两步四则（先乘除后加减） | 两步 |
| 9 | 带括号的三步式，如 `6 × 4 - (15 + 3)` | 括号 |

**0 号牌是唯一的例外**：0 没有伤害、平方也还是 0，按原样就是一张死牌 ——
所以给它「**无伤害，抽 1 张**」，正好是 0 在加法里的角色（加它等于没加）。

题面不是拼字符串拼出来的，而是**先造一棵表达式树**，再分别渲染题面和
递归求值（`battle_scene._build_expr` / `_expr_text` / `_eval_expr`）——
题面和答案用的是同一棵树，**不可能对不上**；括号也按优先级自动补，
`20 - (5 + 3)` 不会渲染成 `20 - 5 + 3`。答案一律是非负整数、除法保证整除。

#### 平方卡：不能单出，只配数字卡

「平方」是**运算卡**（第三种卡类型，卡面紫色、标签「运算」）：

- **不能单独打出** —— 它自己没有任何数值，单出等于白扔一张牌，
  点了会提示「不能单出 —— 要配一张数字卡」
- 和数字卡组合时，题目**先给两个例子**（`2² = 2 × 2 = 4`、`3² = 3 × 3 = 9`），
  再问 `n² = n × n = ?`，要算的就是数字卡上那个数的平方
- **答对之后打出的伤害就是这个平方**：9 号牌 = 81 点，0 号牌 = 0 点
- 平方组合**不额外抽牌**（面积组合会补 1 张）—— 它的补偿就是那个平方本身，
  9² 顶得上好几张牌，再白送一张手牌就没人肯老实打数字卡了

> **怎么拿到它**：三个人起手都没有平方。**打穿第一层层主「正方体·三阶」
> 才会拿到**（`player.SQUARE_UNLOCK_FLOOR`），在解锁之前商店也不卖。
> 这样第一层只能靠 0~9 的加法慢慢磨，第二层起才有 81 点的爆发 ——
> 两张牌的前后差，就是整作难度曲线的分界线。

面积题里的图形**按真实比例画**（底 6 高 4 看着确实是扁的），尺寸标在图上，
公式写在题面（不用背公式）：

| 图形卡 | 图形 | 面积公式 |
|---|---|---|
| 三角盾 | 三角形 | 底 × 高 ÷ 2 |
| 方阵 | 正方形 | 边长 × 边长 |
| 镜像 | 长方形 | 长 × 宽 |
| 反证 | 平行四边形 | 底 × 高 |
| 换元 | 梯形 | （上底 + 下底）× 高 ÷ 2 |
| 归零 | 圆 | 半径 × 半径 × 3（π 取 3） |

- **数字卡上的那个数就是图形上的主尺寸**（底 / 边长 / 长 / 半径）——
  这是「数形结合」四个字的字面意思：数变成形，形再算回数。
- 另一维随机，但保证**面积一定是整数**（三角形 / 梯形的高取偶数）、
  且 ≤ 9999（输入框只有 4 位数字）。
- 认图形题是**四选一**而不是手打中文 —— 720p 窗口里敲中文不现实。
  点选项 或 按 `1`~`4` 选，`回车`提交，`退格`取消选择。

**为什么组合要补一张牌**：一次组合花掉两张卡、两份能量、只占一次出牌机会。
答对时两张卡同时生效之外再多抽 1 张，否则组合永远不如拆成两回合打，
这个机制就只是个摆设。

> 图形卡上没有数字（`value` 恒为 0），所以**单出图形卡不能考算术** ——
> 题目必须跟着"这张卡能提供什么信息"来出，这就是题型分流的由来。

### 手牌类型

| 卡 | 类型 | 效果 |
|---|---|---|
| 0 | 数字 | 无伤害，抽 1 张牌（题目最简单） |
| 1~9 | 数字 | 造成等于牌面数字的伤害（数字越大题越难） |
| 平方 | **运算** | 和数字卡组合：伤害＝数字的平方（**不能单出**） |
| 三角盾 | 图形 | 获得格挡 |
| 方阵 | 图形 | 获得格挡 |
| 镜像 | 图形 | 抽牌 |
| 归零 | 图形 | 清空敌人格挡 |
| 反证 | 图形 | 格挡 + 伤害（构形师专属） |
| 换元 | 图形 | 抽牌 + 伤害（解方程者专属） |

三种类型各有自己的主色（数字蓝 / 图形绿 / 运算紫），战斗卡面、牌组面板、
商店货架**共用同一份配色表**（`player.CARD_TYPE_STYLE`）。

每张卡在卡面中部都有一张**图案**：数字牌的图案就是那个数字本身，
图形卡另有第二种用途：和数字卡组合出**面积题**（见上一节）。

> **平衡现状**：9 号牌 + 平方 = 81 点，第 2 层杂兵 72 血、第 3 层 95 血
> —— 一发 81 点仍然很痛，但已经打不死了（第一层没有平方，杂兵还是 50 血，
> 保持原样不动）。要不要继续按平方的量级往上配，等玩过再定。

### 卡牌图案与敌人形象（`art_shapes.py`）

卡面和敌人**没有任何图片素材**，全是代码画的几何图形 —— 每个图案对应卡名的数学含义：

| 卡 | 图案母题 |
|---|---|
| 0 ~ 9 | 圆盘里一个大数字 —— 数字牌的图案就是这张牌的数字本身 |
| 平方 | 嵌套的两个正方形（x·x） |
| 加一 | 粗加号，四角缀四个小点（"再加一个"）〔老牌面，仍兼容老存档〕 |
| 凑十 | 10 个圆点排成三角阵（1+2+3+4，三角数）〔老牌面〕 |
| 未知数 | 虚线圈 + `χ`〔老牌面〕 |
| 开方 | 一个根号（√），里面压着被开方的横线〔老牌面〕 |
| 三角盾 | 正三角 + 盾脊 |
| 方阵 | 2×2 四个方块 |
| 镜像 | 左右对称三角 + 中间虚线镜面 |
| 归零 | 圆 + 斜杠（∅ 空集） |
| 反证 | `⊥` 记号 + 交点那颗"矛盾点" |
| 换元 | 方块 → 箭头 → 圆 |

| 敌人 | 形象 |
|---|---|
| 几何魔像（普通） | 三角底座 + 方块躯干 + 菱形头（"由最基础的多边形堆成"），身体轻微呼吸 |
| 方程组·三元（精英） | 三个圆盘（各含 `χ`）连成三角互相牵制，中心是还没解出来的"解" |
| 正方体·三阶（第 1 层层主） | 三个正方形**共用一条底边**，边长 1 : 2 : 3（面积 1 : 4 : 9），每块砖左上角点一个"阶"点 |
| 不可解之影（层主） | 收不拢的螺旋 + 留缺口的环（"它本身就是那个矛盾"），缓慢自转 |

层主用哪张画法由 `BattleScene.e_art` 决定：`tower.yaml` 的 `boss_theme: square`
→ `boss_square`，其余按敌人档次 `battle` / `elite` / `boss` 找
（`art_shapes.ENEMY_STYLE` 里四档各一套配色，层主那两档都能画）。

**为什么不用图片**：卡在战斗里只有 132×176、牌组面板里 104×142 ——
这个尺寸下插画细节全糊成一团，几何图标反而最清晰。而且卡名本身就是
数学概念，画成对应图形比画插画更"说人话"。顺带的好处：不用带图、
不用预处理、exe 体积一分不涨。

三处共用同一套图案：战斗手牌（`battle_scene.draw_card`）、牌组面板小卡
（`deck_view._draw_mini_card`）、旧原型（`test_card.py`）。图案函数按
「可用方形边长」的比例绘制，所以大卡小卡形状完全一致，只是缩放不同。

**以后加新卡**：在 `art_shapes._ICONS` 里补一条 `"卡名": 画图函数` 就行。
漏了也不会崩 —— 会画一个问号圆盘，一眼看出"这张卡还没配图案"。
图形卡还要在 `art_shapes.CARD_SHAPE` 里补一条 `"卡名": 形状`，
否则组合出面积题时会退成正方形（不崩，但题目名不副实）。

**题目图形（`draw_shape_figure`）**：面积题 / 认图形题里画的图形，
和卡牌图案共用 `art_shapes.py`。它比卡面图案多两件事：

- 按 `dims` **真实比例**绘制（底 6 高 4 就画成扁的），不是示意图形 ——
  面积题里比例不对，玩家一眼就觉得这题在糊弄人
- 标出尺寸：虚线画「高」，白底小牌写数字（压在图形线上也读得清）；
  认图形题**不标**任何数字（标了像送答案）

**卡名末尾的 `+`**：强化会给卡名加个加号（「三角盾+」），
所以查图案 / 查形状之前一律先过 `base_card_name()` 把加号剥掉 ——
不然玩家花掉一次强化机会之后，卡面图案会变成问号圆盘、组合也认不出形状。

### 战斗背景里的数学装饰（函数画在坐标系上）

战斗背景是「渐变 + 数学装饰」两层，全部纯代码绘制（`BattleScene.draw_backdrop`
与 `BattleScene.draw_math_decor`）。中间那条空档里摆着**两个完整的坐标系**，
函数曲线是在坐标轴上动的，不是飘在半空的裸曲线：

| 图 | 内容 | 怎么动 |
|---|---|---|
| 左 | **y = sin x**（正弦） | 相位随 `t_ms` 推进，整条波**横向流动**（62px/s 左右）；另有一颗「珠子」钉在 x=6.2 处随波起伏 |
| 右 | **y = x²**（抛物线） | 开口系数 `a` 在 0.075~0.175 之间**呼吸**（口大开得缓、口窄立得陡）；一颗珠子沿曲线左右扫描 |

每个坐标系自带：带箭头的 x/y 轴、每 1 单位一道刻度、单位网格（坐标纸）、
原点 `O`、轴名 `x`/`y`，图下方还有一行函数名（`y = sin x` / `y = x²`）。
曲线绘制套在 `screen.set_clip(图纸矩形)` 里，**跑到框外会被裁掉**，
不会有线头飞出去。

**图纸的四条边界是量出来的，不是拍的**（改布局前先看这四条）：

- 左 ≥ 448 —— 三个角色**全部动作帧**的立绘像素并集是 x56~444（实测），再往左会被角色压住
- 右 ≤ 886 —— 最宽的 Boss 半径 100、`E_X=1000`，最左到 x900
- 中间让开 x=640 的「对决区」中线（占 639~641）
- 上下夹在顶栏（y0~52）和地面线（y492）之间

于是能摆图纸的只有 **x448~886 / y126~274** 这一条，两个坐标系就卡在这。
漂浮符号、公式、几何图形的坐标全部提到了模块级常量
（`DECO_SYMBOLS` / `DECO_FORMULAS` / `DECO_SHAPES`），测试脚本按**同一份数据**
检查它们有没有糊到图纸上 —— 符号的漂移公式也提成了 `deco_symbol_offset()`，
所以测试能把整个漂移过程扫一遍，而不是只看一个静帧。

### 界面元素（杀戮尖塔式舞台布局）

- 左右各站一个角色：**玩家在左**（像素立绘，待机动画一直播，
  打出伤害卡播攻击动作、挨打播受击动作；没素材的角色用大号符号圆占位），
  **敌人在右**（几何形象，普通/精英/Boss 体型递增，见上一节）
- **血条和格挡徽章画在角色脚边**（脚点 y=486，血条顶边 y=494），
  脚下有椭圆影子把角色「钉」在地面上
- 敌人**意图**（下回合要干什么，提前告诉你）悬在它头顶
- 左下角：**能量球**（当前/上限）+ **抽牌堆**；右下角：**结束回合**按钮 + **弃牌堆**
- 手牌在底部居中，牌多时自动**向中间叠起来**（牌不会顶到左右两角）
- 顶栏：左侧是角色名·称号，右侧是金币、回合数和 `D 查看牌组` 提示

### 一回合的结算顺序（格挡为什么必须留到敌人出手）

这块原来有个要命的 bug：`end_turn()` 在**敌人出手前**就把 `p_block` 清零了，
于是格挡徽章照常显示、伤害却全额打在身上 —— 玩家看到的就是「防御只有显示、没有效果」。
现在的顺序是：

| 步骤 | 做什么 | 格挡 |
|---|---|---|
| 1 | 玩家出牌（算对题才生效：算术 / 认图形 / 面积） | 打出图形卡时 `p_block += N` |
| 2 | 点「结束回合」 | **保留**，一格都不动；顺手记下 `carry_block` 供【承形】用 |
| 3 | 敌人行动 | **先扣格挡再扣血**：`实际伤害 = max(0, 伤害 - 格挡)` |
| 4 | 新回合开始 | 这时才 `p_block = 0`（格挡不跨回合），随后按【承形】给 2 点 |

> 一句话：**格挡在敌人打完之后、新回合开始时清空**，而不是结束回合时清空。
> 位置错了，整套防御体系就名存实亡。

单卡和组合的**效果结算走同一个 `_settle_card()`**（遗物「约等号」「勾股定理」
和演算者【直感】只有这一份实现）—— 所以组合里的数字卡照样吃【直感】、
组合里的图形卡照样吃「勾股定理」，不会出现两套数值。

### 打完精英 / 层主之后的战利品

普通战斗只掉金币。精英和层主会再多给两样东西，
数量写在 `battle_scene.py` 的 `ENEMY_KINDS` 里：

| 敌人 | 金币 | 遗物 | 强化次数 |
|---|---|---|---|
| 几何魔像（普通） | 28 | — | — |
| 方程组·三元（精英） | 55 | **1 件** | **1 次** |
| 层主（三层都一样） | 120 | **1 件** | **2 次** |
| **第一层层主** | 120 | **1 件** | **2 次** + **解锁「平方」卡** |

面板分**三段**，一路「收下」往下走：

1. **新卡到手**（只有第一层层主有）—— 左边画出「平方」的卡面，
   右边写它的说明，外加一句「层主就是用它打你的」。卡在这一步之前
   **就已经进牌库了**（`main.open_spoils` 里发的），面板只负责把这件事
   大声讲出来 —— 中途关掉游戏也不会把这张牌吞掉
2. **获得遗物**
3. **选一张卡强化**

几个约束：

- 遗物**不会重复发**：`roll_unowned_relic()` 只抽玩家还没有的；
  六件都集齐了就折现成 40 金币（宝箱、商店、事件三处也共用这套逻辑）
- **同一张卡在一次面板里只能强化一次**（不然名字会叠成「加一++」）
- 没有可强化数值的卡（比如「归零」）不能被选中 —— 原来会白加一个 `+` 却不改数值，
  等于骗走一次强化机会
- 路线代价「**开区间**」承诺的「战后额外 1 次强化」也在这里兑现：
  数值先挂在 `player.pending_upgrades` 上（存档会带上），打赢下一场时并进上面那张表

### 血条怎么画的

`BattleScene.hp_bar()` 一个方法同时服务玩家和敌人：

| 细节 | 说明 |
|---|---|
| 位置 | **画在角色脚边**（脚点 y=486，血条顶边 y=494），像踩在地上的状态条 |
| 数字 | 血量数字**压在条里面**，不再单独占一行文字 |
| 颜色 | 按剩余比例变：>55% 绿｜>28% 琥珀｜否则红 —— 不读数字也知道危不危险 |
| 边界 | 0 血 / 负血 / 超上限 / 上限为 0 都不会崩，也不会画出格 |
| 低血 | 填充至少保留一个圆头宽度，否则残血时条会缩成看不见的一点 |
| 描边 | 数字画两层（深色偏移 + 白色），浅底深底都看得清 |

**为什么数字要放进条里**：血条要贴着角色的脚放，空间只有一条，
数字塞进条里才放得下 —— 玩家一眼就能把「血量」和「这个角色」对上。

### 角落的牌堆计数有什么意义

抽牌堆（左下）、弃牌堆（右下）画成小卡叠 + 琥珀色数量角标：
`抽牌堆 + 手牌 + 弃牌堆 = 牌组总张数`。
配合 `D` 牌组面板，出牌/弃牌对不上时能自己核对。

### 音效与背景音乐（`sfx.py`：全程代码合成，零音频文件）

**一个音频文件都没有。** 34 种音效 + 2 首循环 BGM 全部在启动时由
振荡器和滤波噪声算出来（约 1.4 秒，且在后台线程里算，见下）。

为什么不直接放 wav：

| 方案 | 问题 |
|---|---|
| 带 `assets/sfx/*.wav` | 打包时要额外带一整个目录，漏一个文件就少一种声音；几十个音效轻松几 MB |
| 网上找免费素材 | 版权来路不清，这游戏是要发给小朋友玩的 |
| **代码合成** | 零文件、零版权、体积为 0，改音色只改几行参数 |

代价是音色不如专业素材华丽 —— 对这个体量的游戏完全够用，而且
「音效和美术一样全是代码画的」这件事本身和项目的做法是一致的。

#### 「打击感」是怎么做出来的

一个有分量的打击音 = **三层叠加**，少一层都会软：

| 层 | 作用 | 做法 |
|---|---|---|
| 瞬态（attack） | 那声「啪」，负责锐利 | 1~40ms 的高频噪声爆破（`lp` 高 + `hp` 高） |
| 厚度（body） | 那声「实」，负责实体感 | 几百 Hz 下滑到几十 Hz 的三角 / 锯齿波 |
| 重量（sub） | 那声「咚」，负责沉 | 60~110Hz 正弦快速下坠 |

再按伤害量分**四档**（`battle_scene.DMG_TIERS`）——档次越高，噪声越长、
下坠越深、尾巴越久、软件饱和越强：

| 伤害 | 音效 | 震屏 | 听感 |
|---|---|---|---|
| 1~6 | `hit_light` | 1.5px | 清脆的「嗒」 |
| 7~14 | `hit_mid` | 4px | 加了方波「啪」和更深的下坠 |
| 15~29 | `hit_heavy` | 8.5px | 四层叠加 + 饱和，留了余震 |
| 30+ | `hit_massive` | 15px | 蓄力扫频 → 重炮 → 金属余韵（9²=81 就是这一档） |

**这张表同时决定「听到什么」和「抖多厉害」**，两者永远同档 ——
分成两处各判一次，早晚有人改了一处忘了另一处，
然后就会出现「听到轻击、画面却大地震」这种一眼就假的情况。

#### 「吸引小朋友」是怎么做的

| 场合 | 做法 | 为什么 |
|---|---|---|
| 答对 | 上行大调琶音 C5-E5-G5-C6，钟铃音色 + 高音闪烁 | 上行 = 心理上的「成功」，这是有共识的 |
| 答错 | 柔和的下行小三度，音量只有答对的 3/4 | **刻意不用蜂鸣器/警报音**：孩子卡在一道题上本来就沮丧，再来一记刺耳的错误音，下次他就不敢点提交了 |
| 战败 | 缓慢下行，像一声叹息（峰值全档最低之一） | 输一局本来就难受，音效再沉重会让孩子直接关掉游戏 |
| 挨打 | 闷响 + 下行两音，比打击音轻一档 | 被揍不该被声音吓到，让他知道掉血了就够 |
| 奖励 | 金币用经典的两声「叮」（B5→E6） | 电子游戏里最普及的奖励音，条件反射级别的共识，比自己造一个高效 |
| 巨击 | 额外飘一行「厉害！」 | 正向的语言鼓励，家长会喜欢 |
| BGM | 五声音阶（do re mi sol la）+ 简单和弦进行 | 五声音阶**没有半音冲突**，任意两个音同时响都不难听 —— 这是程序生成音乐能听的保险丝 |

#### 什么操作响什么音（反馈地图）

| 位置 | 音效 |
|---|---|
| 主菜单 / 选角 / 存档槽 | 划过按钮 `ui_hover`、点击 `ui_click`、返回 `ui_back` |
| 地图 | 前进 `node_move`、走不到 `ui_deny`、看路径 `ui_hover`、过层 `floor_clear` |
| 遭遇敌人 | `battle_start`（两声低鼓，压在 `node_move` 之后 0.15 秒） |
| 选卡 / 取消 / 出牌 / 抽牌 | `card_select` / `card_deselect` / `card_play` / `card_draw` |
| 出题 / 敲键盘 | `quiz_open`（比出牌音晚 0.12 秒）/ `key_tap`（限流 40ms） |
| 答对 / 答错 | `answer_ok` / `answer_no`；组合题换成更华丽的 `combo_ok` |
| 组合起手 | `combo_charge`（上行蓄力，预告「接下来这一下很疼」） |
| 打中 / 被格挡 | 四档打击音 / `hit_block`（金属「铛」） |
| 上盾 / 清格挡 | `shield`（自己上盾和被人敲是两件事，用两个音） |
| 挨打 / 敌人强化 / 敌人倒地 | `hurt` / `upgrade` / `enemy_die` |
| 新回合 | `turn_start`（听出「又轮到我出牌了」，不用盯回合数） |
| 打赢 / 战败 | `enemy_die` →（0.5s）→ `win` →（1.05s）→ `coin`；`lose` |
| 商店 / 休整 / 宝箱 / 战利品 | `coin` / `heal`+`upgrade` / `relic` / `relic`+`coin` |
| 翻开牌组 | `card_draw`（开）/ `ui_back`（关） |

**通用点击音只给「按钮多」的界面**（UI 场景 + 商店/休整/事件/宝箱面板），
战斗里一律不给 —— 战斗每个操作都有更贴切的专属音，再叠一记咔哒只会糊。
判断收口在 `main._want_click_sound()` 一处。

#### 三个不显眼但必须做对的地方

**1）合成不能堵在启动路上。** 34 条音效算完要 1.4 秒，同步做会让启动
明显卡住。所以交给一个后台线程算，主线程每帧只负责把算好的 PCM
包成 `pygame.mixer.Sound`（建 Sound 必须回主线程，因为要碰混音器）。
线程和主线程之间只通过一个 queue 传不可变数据（名字 + bytes），
共享状态为零 —— 不用锁，也不会「合成到一半被读」。
合成顺序按「玩家最先听到的」排：UI 音最先，BGM 放最后（晚一秒进音乐无感）。

**2）延时的反馈要用游戏时间，不能用墙钟。** 「敌人倒地 → 0.5 秒后号角」
这种排队靠 `sfx.play_after()`，它的倒计时是拿主循环的 `dt` 累减的，
**不是** `time.perf_counter()`。这样它和 BGM 淡入淡出、角色动画走同一条
时间轴：游戏卡一下或被暂停，延时队列跟着一起停，不会「画面还停着、
声音已经抢跑了」。（这里最早就是用的墙钟，测试里第一次抓到。）

**3）限流。** 一秒内连点五张牌，如果每次都出声会叠成「哒哒哒」的噪音。
`play(name, gap_ms=N)` 让同一个音效在 N 毫秒内只响一次：高频操作（选卡、
敲键）设 40~70ms，**关键反馈一律 `gap_ms=0`**（答对、打击必须每次都听得见）。

#### 打击感的视觉搭档

音效只管一半，另一半是画面。三件套都由同一个 `hit_feedback()` 出口负责：

| 反馈 | 做法 |
|---|---|
| 屏幕震动 | 有震动时先画到离屏画布，再整体偏移贴回屏幕 —— 这样**一行绘制代码都不用改**（把参数 `screen` 换成画布即可）。偏移量用两个不同频率的正弦合成，**不能用 random**：随机数每帧乱跳看着像花屏 |
| 受击闪白 | 敌人身上叠三层同心圆（由内往外衰减）。画一个实心白圆会露出一圈生硬的白边，像贴了张白纸 |
| 飘字 | `−81` 这种数字往上飘 + 淡出，**字号随伤害变大**（4 点是 24 号、81 点是 46 号），带白色描边保证压在深色敌人身上也看得清 |
| 整屏泛红 | 玩家挨打时的一层红色（alpha 78，刻意不重 —— 太红会连手牌都看不清） |

震屏幅度在 `hit_feedback` 里**只在离屏画布那一条路上生效**，所以
不抖的战斗一帧都不会多花这块内存（画布是惰性创建的）。

#### 音频开关与优雅降级

`F1` / `F2` 随时开关音效和音乐，状态写在用户数据目录的 `audio.json` 里
（和存档放一起，整个文件夹拷走设置跟着走）。

**没有声卡 / 驱动打不开时，游戏照常能玩** —— `sfx.init()` 会安静地失败，
之后所有 `sfx.play()` 都是安全的空操作。音效挂掉不该让游戏玩不了。

音量平衡：音效总音量 0.85、BGM 0.50，**音乐刻意比音效低一档**。

---

## 六、查看自己的牌组

### 怎么用

**任何时候按 `D`** 都会弹出牌组面板：

| 操作 | 效果 |
|---|---|
| `D` | 打开 / 关闭 |
| `ESC` | 关闭 |
| **滚轮** / `↑` `↓` | 上下翻（牌多的时候） |
| `PageUp` / `PageDown` | 翻一整屏 |
| `Home` / `End` | 跳到最前 / 最后 |
| 点面板外面 | 关闭 |
| 点右下角「关闭」 | 关闭 |

**三种场景都能开**：地图上、节点面板里（商店 / 休整 / 事件 / 宝箱）、战斗中。
在商店买东西之前想看看自己有什么牌，按一下 D 就行。

两个例外（故意的）：

- **战斗中正在答题时不响应** —— 那时候按键要留给数字输入
- **战斗结算画面（胜/负）不响应** —— 那时候按任意键都该是「离开」，
  把 D 吞掉会让玩家按了没反应，很困惑

### 面板长什么样

- 卡片**按类型排好**：数字卡（蓝框）→ 图形卡（绿框）→ 运算卡（紫框）；
  同类型按费用、再按名字 —— 同名卡自然聚成一堆，一眼能看出「我有几张三角盾」
- 左上角圆形数字 = 这张卡要花几点能量
- 顶上写总张数 + 各类型各几张（数字 / 图形 / 运算）
- 牌多时右侧出滚动条，卡片会**裁剪**在面板内（不会画到外面去）
- **面板高度自适应**：牌少就矮一点并居中（开局 16 张不会在下面空一大片），
  牌多则撑到上限 620px 后开始滚动

### 为什么单独一个模块（`deck_view.py`）

这个功能要在**三个不同场景**里用，而它们分属 `main.py` / `battle_scene.py` /
`node_scenes.py`。如果各自画一遍，改一次样式要改三处 ——
本项目已经吃过这种亏：**玩家面板被 `map_scene.MapScene.draw_side()` 和
`main.Game.draw_player_panel()` 各画了一遍**，改前者完全没效果（后者覆盖了它），
当时排查了好一会儿。

所以这次收口：一个 `DeckView` 类，谁想用就
`deck_view.open_with(player.deck, player.char)`，然后把事件丢给 `handle()`、
把绘制丢给 `draw()`。

**而且事件处理放在 `Game.handle()` 最顶上统一做**（而不是各模式各写一份）：

```python
def handle(self, event, mouse):
    # 牌组面板是覆盖层，开着的时候优先吃掉事件 ——
    # 否则在战斗里按 ↑↓ 翻牌组会连带把战场也操作了
    if self.deck_view.open:
        self.deck_view.handle(event, mouse)
        return None
    if event.type == pygame.KEYDOWN and event.key == pygame.K_d:
        ...
```

这样「打开时不能穿透到下层」这条规则只有一份实现，不会出现
「地图上翻牌组会同时滚地图、战斗里翻牌组却没事」这种不一致。

### 顺带加的：顶栏牌堆计数

战斗画面右上角多了 `抽牌堆 N　弃牌堆 N`。单看没什么，配合牌组面板就有用了：

> **抽牌堆 + 手牌 + 弃牌堆 = 牌组总张数**

出牌、弃牌对不上时能自己核对（也是排查「卡牌凭空消失」这类 bug 的快捷手段）。

---

## 七、存档 / 读档

### 三个存档槽

存档不是单个文件，而是**三个槽**（`saves/save_1.json` ~ `save_3.json`），
可以同时保留三局不同的进度。

| 什么时候 | 按什么 |
|---|---|
| 地图上想存一下 | `S` → 弹出槽位列表 → 点一个槽（或按 `1`/`2`/`3`） |
| 想回到上次存的地方 | `L` → 弹出槽位列表 → 点一个有档的槽 |
| 主菜单想接着玩 | 「继续游戏」→ 同样进槽位列表 |
| 死了想回到存档点 | `L`（死亡画面上会提示，也走槽位列表） |
| 死了想从头再来 | `R`（回选角色重开；存档保留，还能 `L` 回去） |
| 死了想回主菜单 | `M` |
| 游戏中途想回主菜单 | `M`（地图 / 节点 / 战斗都能按；建议先 `S` 存一下） |
| 打完层主自动换层 | **自动存一次**（写进本局绑定的那个槽） |

槽位卡片上写着那一局的关键信息：**第几层 / 生命 / 金币 / 牌数 / 存档时间**；
存档模式下点已占用的槽会标红「覆盖」，防止手滑冲掉想留的局。
一局游戏绑定它「第一次存进的那个槽」，之后换层自动存也写到那里。

旧版只有一个 `save.json` 的存档，程序第一次跑 `list_slots()` 时
会自动把它认作「槽 1」（改名为 `save_1.json`），老玩家不丢进度。

### 存档里存了什么

```json
{
  "version": 1,                  // 格式版本，以后改结构用
  "saved_at": "2026-09-15 21:22:17",
  "seed": 485897898,             // ← 关键：地图的随机种子
  "floor_index": 1,              // 在第几层
  "current": 0,                  // 站在哪个节点
  "visited": [0],                // 这层走过哪些节点
  "player": { "max_hp": 80, "hp": 63, "gold": 175,
              "char_id": "calculator",   // ← 关键：玩的哪个角色
              "relics": [...], "deck": [...] },
  "map_effects": { "hide_next": true, "enemy_hp_mult": 1.32 },
  "floor_stats": {}
}
```

### 为什么地图不用整个存下来？

因为地图是「**种子 → 地图**」的确定性函数：**同一个种子跑出来的地图一模一样**。
所以存档只要记住 `seed`（一个数字），读档时重新生成一遍就行 ——
存档文件因此只有 **2~3 KB**，而不是几十 KB。

读档时具体走的是 `save_system._regenerate_map(seed)` →
`build_map.build_in_memory(seed=seed)`，**按存档里的种子把地图原样重建**。

> ⚠️ 这里有个曾经的**假种子 bug** —— 打印出来的种子复现不出它生成的地图，
> 导致读档地图变样。已在避坑记录第 15 条详述，这里不再重复。

**降级路径**：万一存档里**连 `seed` 字段都没有**（很老的存档），
程序会退回读静态 `out/map.json`，只恢复玩家状态 + 楼层 + 站位。
**血和牌不会丢。**

### 容错设计（都有测试覆盖）

| 情况 | 表现 |
|---|---|
| 存档不存在，按 `L` | 提示「读档失败：没有存档」，不崩 |
| 存档 JSON 写坏了 | 提示「存档损坏」，原文件改名为 `save.json.bad` 留档（不直接删） |
| 存档版本比程序新 | 拒绝加载，提示版本号 |
| 存档字段残缺 | 缺的字段用默认值补上，能启动 |
| **存档没有 `char_id`**（老档） | 兜底用 `calculator`（演算者），能启动 |
| 血量是负数 / 超上限 | 自动夹回 `0 ~ max_hp` |
| 牌库里混了坏卡 | 跳过那张坏卡，保留其余 |
| 存档写到一半断电 | 先写 `.json.tmp` 再改名，原存档不受影响 |
| 存档的种子重建不出地图 | 降级重建，只恢复状态，不崩 |

### 已知限制

- **战斗中不能存档**（按 `S` 会提示「战斗中不能存档」）。
  战斗中场面状态太多（手牌、弃牌堆、敌人意图），完整序列化成本高，
  收益却低 —— 爬塔游戏的标准做法就是只在地图上存。
- 存档是**单槽**的，再存一次会覆盖上一份。

---

## 八、怎么发给别人玩（打包）

### 一句话版

```
<python> 打包.py
```

跑完在 `release/数与形-公理塔.zip`（约 25 MB），**直接把这个 zip 发出去就行**。

### 对方拿到之后要做什么

**双击 `NumbersAndForms.exe`。** 就这一步 —— 不用装 Python、不用装 pygame、
不用解压到特定位置、不用联网。

（Windows 会弹一次蓝色的「已保护你的电脑」，那是没买数字签名证书的正常提示，
点「更多信息 → 仍要运行」即可。这一点已经写在给玩家的说明里了。）

### 为什么要打包，不能直接发文件夹

发源码文件夹的话，对方电脑上必须有：
① Python 3.8+　② pygame　③ 一个装有微软雅黑的系统
—— 三个条件缺一个就跑不起来。而「只会双击」的人第一个就卡住了。

打包把这三点全解决了：Python 和 pygame 打进了 exe，
字体也跟着打进去（找不到时还有降级链）。

### `打包.py` 做的五件事

| 步骤 | 做什么 |
|---|---|
| 1 | 重新生成所有 `.bat` 启动器（保证编码与探测逻辑是最新的） |
| 2 | PyInstaller 打成单文件 exe |
| 3 | 整理发布目录：只放玩家需要的东西 |
| 4 | **在隔离目录里真跑一遍**，5 秒不崩才算过 |
| 5 | **读出 exe 里的 PYZ 核对代码指纹**，确认装进去的是最新代码 |

第 4 步是关键 —— 它模拟「对方拿到手」的场景（一个新文件夹里只有一个 exe，
没有源码、没有 `data/`），能抓出「本机能跑、拷过去就崩」的问题。

第 5 步是补另一类漏子：**第 4 步只能证明「能跑」，证明不了「跑的是新代码」**。
增量构建吃到旧缓存、清理被安全策略拦下导致流程中断在半路，
都会让发布目录里躺着上一轮的 exe —— 而它照样能跑 5 秒不崩。
这一步读 PYZ 逐条核对指纹，对不上就直接中止打包，
不会安静地把旧 exe 发出去（原理和三道假信号见第十一章第 24 条）。

### 发布目录里有什么

```
release/数与形/
├── NumbersAndForms.exe     26 MB   双击即玩
├── 发给别人-怎么玩.txt        4 KB   给玩家看的说明（极简版）
├── 0_START_HERE.bat        1 KB   备用启动器（会显示报错信息）
└── 开发者文档.md            40 KB   这个 README 的副本
```

### 单文件 exe 的取舍

`NumbersAndForms.spec` 里刻意选了 **onefile**（只有一个文件）而不是 onedir：

| | onefile | onedir |
|---|---|---|
| 发给别人 | ✅ 一个文件，不会漏 | ⚠️ 一个文件夹，容易漏文件 |
| 启动速度 | 首次慢 1~2 秒（要解压） | 快 |
| 出问题排查 | 较难 | 容易 |

对「只会双击的人」来说，**一个文件**比启动快两秒重要得多。

### 想看到报错信息（调试版）

默认 exe 是**无控制台**的（`console=False`），好处是不弹黑框，
代价是 Python 的报错看不见。要排查问题就临时改：

```python
# NumbersAndForms.spec 里
console=True,      # ← 默认 False
```

重新打包，运行时就会弹一个黑框把报错显示出来。

### 三个针对打包的改造（都是必须的）

打包后 `__file__` 和执行环境都变了，有三处不改就会出问题。
全部收在 **`game_env.py`** 一个文件里：

| 问题 | 不改会怎样 | 解法 |
|---|---|---|
| `__file__` 指向临时解包目录 | **存档写进临时目录，退出即删** | `user_data_path()` → 优先 exe 同级的 `saves/` |
| 资源被解压到别处 | 读不到 `tower.yaml` | `resource_path()` → `sys._MEIPASS` |
| 别人电脑没微软雅黑 | **启动就崩** | 多级字体兜底链 + 最终退回 pygame 内置字体 |

**约定**：任何地方要用字体或路径，都从 `game_env` 拿，别再自己写
`Path(__file__).parent` 或 `"C:/Windows/Fonts/..."`。

### 重启打包（改了代码之后）

```
<python> 打包.py
```

会覆盖 `release/` 里的内容。`build/` 和 `dist/` 是中间产物，已在 `.gitignore` 里。

---

## 九、目录结构

```
游戏制作尝试/
├── 0_START_HERE.bat        ← 双击这个（菜单）
├── 3_run_game.bat            主游戏启动器（开场界面 → 地图 → 节点 → 战斗 → 下一层）
├── main.py                 ★ 主程序入口（场景流转：菜单 / 选角色 / 载入 / 游戏）
├── ui_scenes.py            ★ 开场三个界面（主菜单 / 选角色 / 载入中）
├── game_env.py             ★ 路径与字体的统一入口（打包后靠它还能跑）
├── deck_view.py            ★ 「我的牌组」覆盖层（地图/节点/战斗通用，按 D 开）
├── map_scene.py            ★ 地图场景（爬塔、选路线、路线代价、遗物图鉴 + 查找）
├── node_scenes.py          ★ 非战斗节点面板（休整点 / 商店 / 事件 / 宝箱）
├── battle_scene.py         ★ 战斗场景（可被调用的战斗类）
├── char_art.py             ★ 角色立绘与动作动画（读 assets/chars/，缺素材不崩）
├── art_shapes.py           ★ 卡牌图案 + 敌人形象（纯代码几何绘制，无贴图）
├── sfx.py                  ★ 音效 + 背景音乐（全程代码合成，一个音频文件都不要）
├── player.py               ★ 玩家状态 + 三名角色定义（CHARACTERS）
├── save_system.py          ★ 存档 / 读档（SaveManager + 版本号 + 容错）
├── assets/                   美术资源（打包时一起进 exe）
│   └── chars/                  三个角色的立绘 + 动作帧（idle / attack / hit）
│       ├── calculator/           演算者（源素材：人物(1)/角色001）
│       ├── geometer/             构形师（源素材：人物(1)/角色002）
│       └── solver/               解方程者（源素材：人物(1)/角色003）
├── 人物(1)/                  美术源文件（Aseprite 工程 + 原始 GIF，不进 git）
├── saves/                    存档目录（自动创建，已被 .gitignore 忽略）
│   ├── save_1.json             存档槽 1（共 3 个槽，约 2~3 KB/个）
│   └── save_3.json             …（save_2.json 同理）
├── 2_run_map.bat             单独跑地图
├── 1_run_card.bat            单独跑战斗
├── test_card.py              独立版卡牌战斗（答题才出牌）
├── 4_run_my_test.bat         红方块窗口
├── my_test.py                最早的红方块窗口
├── 打包.py                  ★ 一键打成免安装 exe（详见第八章）
├── NumbersAndForms.spec      PyInstaller 打包配置
├── 发给别人-怎么玩.txt         给玩家看的极简说明
├── release/                  打包产物（已被 .gitignore 忽略）
│   ├── 数与形/                 发布文件夹（exe + 说明）
│   └── 数与形-公理塔.zip        ← 直接把这个发出去
├── tools_dev/                开发用脚本（不参与打包）
│   ├── gen_bat.py            生成所有 .bat 启动器（GBK + 探测 Python）
│   └── gen_start_menu.py     生成 0_START_HERE.bat
├── README.md                 本文件
├── tmp/                      验证脚本与截图（已被 .gitignore 忽略）
│   ├── verify_ui.py          开场界面验证（无头，52 项断言）
│   ├── verify_nodes.py       六种节点全量验证（无头，63 项断言）
│   ├── verify_save.py        存档读写验证（无头，55 项断言）
│   ├── verify_keys.py        按键链路 + 滚动方向 + 相机边界（无头，48 项断言）
│   ├── verify_slots.py       多存档槽 + M 回主菜单（无头，25 项断言）
│   ├── verify_scroll_deck.py 滚动方向 + 血条 + 牌组面板（无头，70 项断言）
│   ├── verify_art_shapes.py  卡牌图案 + 敌人形象（无头，41 项断言）
│   ├── verify_bg_decor.py    背景坐标系 + 函数曲线（无头，87 项断言）
│   ├── verify_combat_rules.py 格挡结算 + 三角色被动 + 战利品 + 遗物（无头，63 项断言）
│   ├── verify_shape_quiz.py  组合出牌 + 面积题 + 认图形题（无头，138 项断言）
│   ├── verify_number_quiz.py 数字牌 0~9 + 难度阶梯 + 平方组合 + 组合优惠（无头，123 项断言）
│   ├── verify_sfx.py         音效合成 / 音色设计 / 反馈接线 / 打击感分档（无头，149 项断言）
│   ├── verify_floor_progress.py 商店删牌规则 + 三层难度递增 + 层主与平方解锁（无头，76 项断言）
│   ├── verify_fingerprints.py 打包前先在本机源码上核对代码指纹（35 条）
│   ├── smoke_game.py         端到端冒烟（21 项断言）
│   ├── render_shots.py       批量渲染各面板截图
│   ├── render_ui_shots.py    渲染开场界面截图
│   ├── render_round_shots.py 渲染本轮改动（血条 / 牌组面板，10 张图）
│   ├── render_quiz_shots.py  渲染三种题型（算术 / 面积 / 认图形）+ 组合提示
│   ├── render_number_quiz_shots.py 渲染数字牌卡面 / 各难度档题目 / 平方题（9 张图）
│   ├── render_sfx_shots.py   渲染打击感的视觉反馈（飘字 / 震屏 / 闪白 / 泛红）
│   ├── render_floor_shots.py 渲染商店删牌 / 平方解锁 / 正方体·三阶（本层改动）
│   ├── render_cam_extremes.py 渲染「滚到顶 / 滚到底」的构图（防空屏回归）
│   ├── verify_exe_contents.py 核对 exe 里的代码指纹（确认打进包的是新代码）
│   ├── render_save_shots.py  渲染存档相关界面截图
│   ├── render_card_enemy_shots.py 渲染卡面图案与三档敌人形象
│   ├── render_art_patterns.py 单独渲染图案网格（先看形状再接进战斗）
│   ├── render_bg_decor.py    渲染背景装饰层（整屏 + 裁中央放大两帧，看动画）
│   ├── render_fix_shots.py   渲染战利品面板与格挡生效的战斗截图
│   └── prepare_char_art.py   把 人物(1)/ 的 GIF 拆成 assets/chars/ 的 PNG 帧
└── map_tools/                地图数据与生成工具
    ├── 2_gen_map.bat         重新生成地图
    ├── data/                 配置表（YAML）
    │   └── tower.yaml          三层塔配置：每层的层主名字/血量、难度倍率
    │                           enemy_scale、以及 boss_theme（改这个来调平衡）
    ├── tools/
    │   ├── build_map.py        地图生成器
    │   │                       （build_in_memory 内存生成 / build 落盘）
    │   │                       （--random 每次不同 / --seed N 可复现）
    │   ├── render_map.py       生成静态预览网页 → out/map.html
    │   └── sample_distribution.py  跑 60 轮抽样，检查分布是否合理
    ├── 3_gen_map_random.bat  随机生成地图（每次都不一样）
    ├── out/                  生成结果（自动创建）
    └── 使用说明.md            地图工具详细说明
```

### 重要：游戏的地图是「运行时现场生成」的

**游戏本身不读 `out/map.json`。** 每次开新局，`Game` 初始化时会调用
`build_map.build_in_memory()` **现场生成一张新地图**（不落盘），
所以**每开一局节点布局都不一样**。

`map_tools/out/map.json` 现在的用途只剩两个：

1. 给 `map_tools\2_gen_map.bat` / `3_gen_map_random.bat` 落一份档
2. 生成 `out/map.html` 预览页，方便不开游戏也能看地图长什么样

```
build_map.py
    ├── build_in_memory(seed)  → 返回 dict（不落盘）  ← 游戏用这个
    └── build(seed)            → 调上面那个 + 写 out/map.json + 打印种子
```

生成逻辑只有一份（在 `build_in_memory` 里），两个入口共用，
不会出现「预览图和游戏里的图不一样」这种事。

**为什么要这么改**：以前 `Game` 读的是静态的 `out/map.json`，
而这个文件只在手动跑生成器时才变 —— 于是「不管运行几次，
地图节点都是一样的」。用户反馈过这个现象，根因就在这。

**存档怎么复现地图**：存档记 `seed`，读档时调
`build_in_memory(seed=存档里的seed)` 重新生成同一张图（详见第七章）。

**改生成器之后还需要重跑 `.bat` 吗？**
只是为了更新 `out/map.json` 和 `out/map.html` 的话要；**游戏本身不用**，
因为游戏每次都是现算的。但记得跑一遍抽样脚本确认分布还正常。

---

## 十、地图工具

运行 `map_tools\2_gen_map.bat`，会：

1. 读取 `data/tower.yaml` 里的三层塔配置
2. 生成节点与连边 → `out/map.json`
3. 画成一张 SVG 网页 → `out/map.html`，并**自动用浏览器打开**

### 想要一张新地图？

三种模式，**双击 `map_tools\3_gen_map_random.bat` 就是每次全新的地图**：

| 想做什么 | 怎么做 |
|---|---|
| **每次都不一样**（推荐） | 双击 `map_tools\3_gen_map_random.bat`，或命令行 `--random` |
| 固定那张调试用的图 | 双击 `map_tools\2_gen_map.bat`（默认种子 `20260915`） |
| 复现某张特定的图 | 命令行 `--seed 1234`，种子号会打印出来 |

生成时会打印**随机种子**，想再抽到同一张图就把它填给 `--seed`。

> 📌 **这只是「预览」用的。** 游戏自己不看这张图 ——
> 开游戏时它现生成，所以**不跑这个 `.bat` 也已经每次都不一样了**。
> 这里生成的 `map.json` / `map.html` 是给你在工作之余
> 不开游戏、用浏览器直接看地图用的。

### 节点类型怎么撒的

这一节前后改过**三版**，把演进过程记下来，免得以后又绕回去。

**第 1 版：逐行独立掷骰子 + 每 5 行指定一个「特殊行」。**

结果是**每行几乎都是「3 个战斗 + 1 个事件」**，特殊行又变成清一色
（`=休整 √商店 √商店 √商店`）。根因是普通行的候选池排除了
`rest/shop/treasure` 但**漏了 `event`**，而 `event` 权重不低，
抽 4 个位置几乎必然中一个。

**第 2 版：先按权重算整层配额，再打散撒进各行。**

修掉了上面的 bug，但引入了新的「太整齐」问题：

- 每层节点数几乎一样（实测恒定 32/35/38），因为行数固定、
  `randint(2, width)` 的期望值恒定
- 每种特殊节点数量被配额钉死，每层都是「休整 2、商店 2、事件 2~3」
- 强制「每行最多 1 个特殊节点」，特殊节点像打格子一样均匀铺开

用户的原话是「**地图随机性不行，再改改**」—— 问题不是"缺项"，
而是**方差太小**，观感上觉得每次都一样。

**第 3 版（现在）：每格独立掷骰子 + 轻量约束。**

随机性交给概率自然生长，约束只在「明显不合理」时兜一下：

1. **每行几个节点：真的随机**（1~`width`，允许只有 1 个的
   「独木桥」行，那是很好的节奏变化）
2. **每个格子：从全部类型按 `weight` 掷骰子**（含特殊节点）
3. **软约束**：同一行不出现两个同类型特殊节点；
   相邻两行不连续出同一种特殊节点；一行最多 2 个特殊节点
4. **硬兜底**：掷完之后统计每种特殊节点的总数，
   低于下限就补、高于上限就换成普通战斗

另外还做了两处「手感修正」：

- **开局第一回合必须有得选**：起点行只有 1 个节点，它连到第二行
  是按 x 位置就近连。如果第二行只有 1 个节点，那「第一回合选路」
  这个动作就不存在了。所以强制第二行 `>= 2` 个节点，
  并且把最无聊的格子换掉 —— 一个换休整点、一个换商店，
  于是开局永远是「**稳一手 or 拼一把**」。
- **连边数量也随机**：1 / 2 / 3 条按 `40% / 45% / 15%` 掷，
  原来固定 2 条看起来很规整。

实测 60 轮抽样（`map_tools/tools/sample_distribution.py`）：

| 检查项 | 结果 |
|---|---|
| 地图唯一性 | 60 / 60 张互不相同 |
| 每层节点总数 | 20 ~ 43（标准差 4.08） |
| 精英战斗数量 | 0 ~ 12（标准差 2.05） |
| 商店数量 | 1 ~ 5（标准差 1.00） |
| 缺项 | 三层 × 四类型全部出现过 |
| 扎堆（一行 ≥3 个特殊节点） | 0 次 / 2520 行 |
| 死路 / 走不到 Boss | 0 个 / 0 张 |

对比第 2 版（节点数恒定 32/35/38、精英恒定 5/6/5），
现在每层的规模、精英密度、特殊节点位置都在自然波动。

当前输出（固定种子 `20260915`）：

| 层 | 名称 | 节点 | 连边 | 带代价的边 |
|---|---|---|---|---|
| 1 | 塔基 · 数之庭 | 21 | 27 | 5 |
| 2 | 塔身 · 形之廊 | 32 | 45 | 7 |
| 3 | 塔冠 · 方程之顶 | 33 | 49 | 7 |

（用随机模式每次数量和连边都会变。）

**改完生成器要做什么？**

- **游戏不用管** —— 它每次开新局都现算（见第九章）
- 想让 `out/map.json` / `out/map.html` 这个**预览**跟着更新，
  重跑一次 `map_tools\2_gen_map.bat`
- 顺便跑一下 `map_tools/tools/sample_distribution.py` 确认分布没坏

> 历史上这里是「改完必须重跑，否则游戏里还是旧图」，
> 那个坑正是后来改成运行时生成的动机（见避坑记录第 13 条）。

---

## 十一、避坑记录（踩过的坑，别再踩）

### 1. `.bat` 报 `Code language not supported or defined`

**现象**：双击脚本，窗口一闪，报这行错。

**原因**：`cmd.exe` 按 **GBK** 解码 `.bat` 文件，但文件是 **UTF-8** 存的。
里面的中文变成乱码，乱码行里的 `chcp 65001` 也无法识别，于是报错。

**解决**（已在本项目全部改好）：
- `.bat` 内容**只用 ASCII 字符**（纯英文提示）
- `.bat` 文件用 **GBK 编码**保存
- **不写 `chcp 65001`**

这样在任何中文 Windows 上双击都正常，且不需要管理员权限。

> 如果以后要加中文提示，正确做法是用 GBK 编码写 `.bat`，
> 而不是改成 UTF-8 再 `chcp`。

### 2. 中文文件名的 `.bat` 引用

`测试.py` 已重命名为 `my_test.py`。
纯 ASCII 的 `.bat` 引用中文文件名会因编码不一致找不到文件，统一用英文名最稳。

### 3. 用哪个 Python

所有脚本用的是项目自带的隔离环境：

```
C:\Users\sanji\.workbuddy\binaries\python\envs\naf\Scripts\python.exe
```

已装好 `pygame 2.6.1` 和 `PyYAML 6.0.3`。
不用管系统的 Python，也不用额外装包。

### 4. `draw()` 撞名把自己覆盖掉（本轮踩的最狠的坑）

**现象**：`BattleScene` 一构造就报
`TypeError: BattleScene.draw() missing 2 required positional arguments`。

**原因**：类里有两个 `draw` ——

```python
def draw(self, n):                        # 抽 n 张牌
def draw(self, screen, mouse, t_ms):      # 画一帧
```

Python 后定义的**直接覆盖**前一个，于是 `self.draw(5)`（想抽牌）
调到了「画一帧」上，参数对不上。

**解决**：抽牌方法改名 `draw_cards()`。
**教训**：同一个类里别让「动作」和「渲染」共用 `draw` 这个名字，
渲染统一叫 `draw` / `draw_xxx`，其他动词换名字。

### 5. 布局写在 `draw` 里 → 第一帧点不到

**现象**：面板刚出现时点按钮没反应，得等一帧之后才正常。

**原因**：主循环是 `handle()` → `draw()` 的顺序。
`handle` 里要用 `self.btn_ok`，但它是**上一帧** `draw` 才创建的 ——
第一帧根本还不存在（或还是空的）。

**解决**：所有「点击判定要用到的矩形」都在 `__init__` 里算好；
如果矩形依赖运行时的状态（比如牌库变了），就在**改变状态的那一步**
同步重算，绝不放在 `draw` 里。

本项目已按这个原则改过 4 处：
`ShopPanel.item_rects` / `ShopPanel.card_rects` /
`RestPanel.card_rects` / `EventPanel.opt_rects` / `TreasurePanel.btn_ok`。

### 6. 打死敌人不结束战斗

**现象**：一刀把敌人砍到 0 血，战斗却没结束，要再点「结束回合」才判胜。

**原因**：`resolve_card()`（出牌结算）里忘了调 `check_end()`，
只有 `submit_quiz()` 和 `enemy_act()` 里调了。

**解决**：`resolve_card()` 末尾补上 `self.check_end()`。
**教训**：凡是会改动血量/胜负的入口，都要过一遍结束判定。

### 7. 卡片网格排到屏幕外

**现象**：休整点一进去，牌库网格的 x 从 `-119` 铺到 `1399`，左右都出屏了。

**原因**：布局用 `total = n * cw + (n-1) * gap` 居中，
10 张卡 × 132 宽早就超过 1280，居中的结果是**两边一起溢出**。

**解决**：改成**按行折行**（`per_row = (WIDTH - 120) // (cw + gap)`），
并且只在真正进入选卡界面时才排布。

### 8. 无头验证 pygame 的正确姿势

改完代码怎么确认没坏？**别靠肉眼看窗口**，用无头跑：

```python
os.environ["SDL_VIDEODRIVER"] = "dummy"   # 无显示器
os.environ["SDL_AUDIODRIVER"] = "dummy"

# 投递「真」事件（唯一能被 event.get() 消费的方式）
pygame.event.post(pygame.event.Event(
    pygame.MOUSEBUTTONDOWN, {"pos": (x, y), "button": 1}))
for ev in pygame.event.get():
    scene.handle(ev, (x, y))
```

- ❌ 假事件（`pygame.event.get = lambda: [...]`）**不会被消费**，
  连续三帧渲染结果一模一样，看着像"没 bug"，其实什么都没触发。
- ✅ 想检查「没崩」，直接调 `screen.fill()` + `scene.draw(screen, mouse, t)`；
  想检查「画面长什么样」，用 `pygame.image.save(screen, "x.png")` 存图再看。
- ⚠️ 项目里有 `tmp/verify_nodes.py`（63 项断言）和 `tmp/render_shots.py`（批量截图），
  改完节点/战斗代码建议都跑一遍。

### 9. 地图「每次都长一样」

**现象**：不管重跑多少次，地图永远是「3 个战斗 + 1 个事件」，一模一样。

**两个原因，要一起改**：

**(a) 种子写死了**
`build_map.py` 里 `SEED = 20260915` 是个常量，`random.Random(SEED)`
每次都从同一状态出发 → 逐字节相同的结果。
→ 改成 `--random` 时传 `None`（用系统随机源），并且**把实际用的种子打印出来**，
方便复现。

**(b) 类型分配是「逐行独立掷骰子」**
这是「3 战斗 + 1 事件」的真正来源：

```python
elif r % 5 == 4:                     # 每 5 行硬性一个特殊行
    ntype = rng.choice([...特殊节点...])
else:
    ntype = weighted_choice(         # ← 池子里把 rest/shop/treasure 排除了
        [t for t in node_types
         if t["id"] not in ("rest", "shop", "treasure")], rng)
```

普通池里只剩 `battle`(45) 和 `elite`(15) 和 `event`(12)……
等等，`event` **不在排除名单里**，所以权重 45:15:12 抽 4 个位置，
出来的就是「3 个 battle + 1 个 event」。
特殊行更惨：第一个位置定死是特殊节点，**剩下 3 个位置仍然从排除过特殊节点的池子里抽**，
于是变成 `=休整 √商店 √商店 √商店`（一行三家商店）。

**→ 改成「先规划整层配额，再打散撒进各行」**：先按权重算好每种要几个
（各带上下限、且浮动 ±1），把它们分到**不同行**（每行最多 1 个、
尽量互相隔开），剩下的位置才填普通/精英。

**顺带一个坑**：`boss` 不在 `tower.yaml` 的 `node_types` 里
（它是层主专属），所以 `type_by_id["boss"]` 会 `KeyError`。
→ 用 `.get()` 兜一个默认值。

### 10. 校验地图分布要「抽样」，不能只跑一次

只看一次生成结果，没法判断随机性够不够、有没有缺项。
`map_tools/tools/sample_distribution.py` 会跑 40 轮，检查四件事：

| 检查 | 期望 |
|---|---|
| 40 轮的地图是否互不相同 | 40/40 唯一 |
| 三层 × 四类型是否都出现过 | 零缺项 |
| 「一行 ≥3 个特殊节点」 | 0 次 |
| 每种特殊节点的数量区间 | 有波动 |

改完生成器建议跑一下，比肉眼看十来张图靠谱。

### 11. `handle()` 里忘记 `return`，信号全被吞掉

做存档时踩的坑，**症状很隐蔽**：按 `ESC` 不退出、按 `L` 不读档，
但程序不报错、界面也照常刷新 —— 看起来就像「按键没绑上」。

根因是顶层派发函数写成了这样：

```python
def handle(self, event, mouse):
    if self.mode == "map":
        self.handle_map(event, mouse)      # ← 返回值被丢了
    elif self.mode == "node":
        self.handle_node(event, mouse)
```

`handle_map()` 明明 `return "quit"` / `return ("replace", new_game)`，
但 `handle()` 没把它传出去，主循环永远收不到信号。

**→ 层层都要 `return`**。这条现在由 `tmp/verify_keys.py` 守着
（直接投递真 `S` / `L` / `ESC` 键，检查主循环拿到的返回值）。

**教训**：这类「返回值被吞」的 bug，看代码很难发现（缩进一样、
没有语法错误），**必须用测试从外部打进去**才能暴露。

### 12. 无头测试别用「真存档路径」

第一版 `verify_save.py` 里写 `g = M.Game()` 之后忘了替换 `save_mgr`，
结果测试跑完在项目根下留了个 `saves/save.json`；更早一版还在
`SaveManager.save()` 里硬编码了 `SAVE_DIR.mkdir()`，导致**即使用临时路径
也会凭空建出一个空的 `saves/` 目录**。

**→ 两条规矩**：
1. 测试一律用 `SaveManager(path=临时目录/xxx.json)`，别碰真存档
2. 建目录要建**「这份存档所在目录」**（`self.path.parent`），
   不是模块级那个常量 —— 否则自定义路径的存档会顺手污染默认位置

现在 `verify_save.py` 每次跑完临时目录都会被清掉，
项目根下不会多出任何东西。

### 13. 改了生成器不重跑，游戏里看到的还是旧地图

`map.json` 是 `tools/build_map.py` **生成出来的静态文件**。

> ⚠️ **这一条现在只对「预览」有效了。**
> 游戏已经改成运行时现场生成（见第九章），所以改完 `build_map.py`
> 直接开游戏就能看到新图。重跑 `.bat` 的意义只剩更新
> `out/map.json` / `out/map.html` 这两个预览产物。

历史上被这个坑了一次：改完随机性跑测试全绿，
结果游戏里还是老样子，因为游戏读的是没更新的静态文件。
**这个坑本身，正是后来「改成运行时生成」的动机。**

动过 `build_map.py` / `tower.yaml` / 任何 `data/` 里的配置，想更新预览就跑：

```
map_tools/2_gen_map.bat
```

（等价于 `python map_tools/tools/build_map.py`）

### 14. 「修好了」要用数据证明，还得盯住改动引入的新问题

本轮修「一行 ≥3 个特殊节点」时踩了连环坑，值得记一下：

**第一坑：以为改的是 A，其实 B 才是源头。**

我先把 `plan_floor_types()` 改了半天，扎堆数从 18 降到 0，
很满意地重跑其他验证 —— 结果一测「第一层能不能走到精英」，
发现 **0/30**：第一层的特殊节点全挤在开局三行，
后面全是战斗，走完整层遇不到几次。**修好了扎堆，修坏了节奏。**

**第二坑：真正制造扎堆的是另一段代码。**

后来写了个调试脚本（`tmp/debug_cluster.py`）分别统计
「`plan_floor_types` 的输出」和「最终节点」里的扎堆数，结论很干脆：

```
扎堆出现阶段统计：
  plan_floor_types 输出就有    0
  最终节点才有                 68
```

也就是说规划函数是干净的，扎堆是**后面 `generate_floor()` 里
那段「开局保证」代码**造的 —— 它只数了「战斗/精英够不够 2 个」，
没按整行宽度算预算，于是 `[事件, 宝箱, 战斗]` 这种行也被补成 4 个特殊节点。

**→ 三条规矩**：
1. **改动前先写个脚本把「哪一步产出的」钉死**，别凭猜改。
   分段统计（规划函数的输出 vs 最终节点）比读代码快得多。
2. **一处修完，另外几处必须全部重跑**。「60 轮抽样全绿」
   不能替代「三层都能走到精英」。
3. **加了新约束，就要回头检查旧代码有没有绕过它**。
   我加了「一行最多 2 个特殊节点」，但第 3 步的补位逻辑
   和开局保证逻辑都能绕过它 —— 补位那个还因为
   「一次挑够 N 个再一起改」导致计数没刷新，同一行被连补 2 个。
   改成**逐个补、每补一个重排一次候选**才对。

### 15. 「报告的随机种子」必须真的能复现它生成的东西（最隐蔽的一个）

**现象**：存档 → 读档 → **地图变样了**。
而更诡异的是：`seed` 一模一样、节点总数一模一样，
**但节点类型对不上**。

排查顺序（值得记，因为前几步都是"看起来正常"）：

1. 怀疑模块被重复加载（`build_map` 被 import 了两遍，`random` 状态串了）
   → 检查 `bm2 is M.B` → `True`，**排除**
2. 怀疑 `build_in_memory(seed=N)` 同种子不确定
   → 连跑 3 次比对 → 完全一致，**排除**
3. 于是只剩一种可能：**「报告的种子」根本不是「用来播种的种子」**
   → 验证：拿打印出来的 seed 重新生成，比对原图 → **对不上**，抓到了

**根因**是这一行：

```python
rng = random.Random(seed)
used_seed = seed if seed is not None else rng.randint(1, 999999999)
```

`seed is None` 时，`rng` 是由**系统熵**播种的，
`used_seed = rng.randint(...)` 只是「从这个随机流里再抽一个数」——
**它跟 rng 的实际状态没有任何关系**。于是：

- 程序打印 `随机种子：532265712`
- 而 `random.Random(532265712)` 生成出来的是**另一张图**
- 存档里记的就是这个假种子 → 读档读到的自然不是同一张

**为什么以前没暴露**：静态地图时代，读档读的是 `out/map.json`，
根本不走种子重生这条路，假种子也就无从暴露。
**改成运行时生成之后，问题立刻浮出来了** —— 这个 bug 其实从做存档时
就存在（提交 `00a6a76`），只是躺了三个提交。

**→ 正确写法**：

```python
if seed is None:
    seed = random.SystemRandom().randint(1, 999999999)   # 先定种子
rng = random.Random(seed)                                # 再拿它播种
```

**→ 规矩**：
1. **「记录种子」要在「用它播种之前」完成**。顺序反了就拿到假种子。
2. 凡是「靠种子复现」的东西，**必须写一条测试：拿到报告的种子，
   重新生成一遍，逐字段比对**。只测「同一种子跑两次一致」是不够的 ——
   那个测试在上面第 2 步就通过了，但 bug 依然在。
   现在的 `verify_ui.py` 第 7 组专门守这条。

### 16. 同一个面板被两处画，改一处没用

**现象**：给角色加了名字，改完 `map_scene.draw_side()` 里的 HUD 面板，
渲染截图一看 —— **还是「演算者」**，改动像没生效。

**根因**：玩家状态面板画了**两遍**：

- `map_scene.MapScene.draw_side()` 里画一遍（地图自带的简化版）
- `main.Game.draw_player_panel()` 里**又画一遍**（覆盖在同一个位置）

后者把前者完整盖住了，所以只改前面那处，屏幕上什么都不会变。

**→ 规矩**：
1. 改一个「看起来应该在这里」的面板却没效果时，**先搜这个面板的其它绘制点**，
   别急着怀疑代码没保存。
2. 更彻底的做法是**只留一处**。当前保留了两处是因为
   `map_scene` 要能脱离 `Game` 单独跑（`2_run_map.bat`），
   所以两边各自有兜底 —— 代价就是改的时候必须**同步改两处**。

现在的 `verify_ui.py` 第 2 组会直接断言「HUD 里出现角色名 · 称号」，
两处漏改一处就会被测出来。

### 17. 打包成 exe 后，`__file__` 和「项目根」全都变了 ⚠️

**这是打包最容易踩、也最致命的一个坑**，因为它表现为
「本机跑得好好的，发给别人就崩 / 存档莫名其妙丢了」。

**三个具体症状**：

**(a) 存档写进临时目录，退出即删**
源码模式下 `Path(__file__).parent` 就是项目根，写 `saves/` 没问题。
打包后 PyInstaller 会把内容解压到 `%TEMP%\_MEIxxxxxx\`，
`__file__` 指向**那里** —— 存档写进去，程序一退就被清掉，
玩家下次打开发现存档没了（而且完全不报错，最难查）。

**(b) 读不到数据文件**
`tower.yaml` 如果没打进包，或者代码按 `__file__.parent.parent/"data"` 去找，
打包后路径算出来是临时目录里的一个不存在的路径 → 地图生成直接失败。

**(c) 别人电脑上没有微软雅黑 → 启动就崩**
`pygame.font.Font("C:/Windows/Fonts/msyh.ttc", 20)` 在字体文件不存在时
**抛异常**。自己电脑上有雅黑，永远发现不了这个问题。

**解法：所有路径和字体都收进一个 `game_env.py`**

```python
IS_FROZEN = getattr(sys, "frozen", False)   # PyInstaller 会设这个标志

def app_dir():          # exe（或项目根）所在目录 —— 用户看得见的地方
    return Path(sys.executable).resolve().parent if IS_FROZEN \
           else Path(__file__).resolve().parent

def resource_path(*p):  # 只读资源：打包后在 _MEIPASS 里
    base = Path(getattr(sys, "_MEIPASS", app_dir())) if IS_FROZEN \
           else Path(__file__).resolve().parent
    return base.joinpath(*p)

def user_data_path(*p): # 可写数据：优先 exe 同级，写不了退 AppData
    ...

def load_font(size):    # 多级兜底，最后退回 pygame 内置字体，永不抛异常
    ...
```

**→ 三条规矩**：
1. **路径只从一个地方拿**。禁止在业务代码里再写
   `Path(__file__).parent` 或 `"C:/Windows/Fonts/..."`。
   散布在 6 个文件里的 31 处字体调用，打包时会全部变成坑。
2. **区分「只读资源」和「可写数据」**。前者在临时解包目录，
   后者必须在用户看得见的地方（exe 同级 or AppData）。
3. **字体要有降级链，永不抛异常**。没有中文字体时退回 pygame
   内置字体 —— 中文会显示成方块，但**程序能跑、能点、能退出**，
   比直接崩掉强得多。

**验证方式**：把 exe 单独拷到一个**空文件夹**里跑，
检查 ① 能启动 ② 存档落在同级的 `saves/`。
`打包.py` 的第 4 步就是自动做这件事。

### 18. `.bat` 里写死本机 Python 路径，别人电脑上必然报错

**现象**：自己双击 `.bat` 一切正常，发给别人报
「系统找不到指定的路径」。

**根因**：

```bat
"C:\Users\sanji\.workbuddy\binaries\python\envs\naf\Scripts\python.exe" "main.py"
```

这条路径只存在于开发机上。

**解法**：`.bat` 里做**四级探测**，按可靠性从高到低试：

```bat
set "PYEXE="
rem 1) 项目自带的虚拟环境（如果一起发过去了）
if exist "%~dp0.venv\Scripts\python.exe" set "PYEXE=%~dp0.venv\Scripts\python.exe"
if defined PYEXE goto RUN
rem 2) PATH 里的 python
for %%I in (python.exe) do if not defined PYEXE if exist "%%~$PATH:I" set "PYEXE=%%~$PATH:I"
if defined PYEXE goto RUN
rem 3) py 启动器
where py >nul 2>nul
if %errorlevel%==0 set "PYEXE=py"
if defined PYEXE goto RUN
rem 4) 常见安装位置 ...
rem 都没有 -> 打一段英文提示（不能是中文，见第 1 条）并 exit /b 1
```

**顺带**：`.bat` 是**生成出来的**（`tools_dev/gen_bat.py`），不是手写的。
这样能保证：① 编码永远对（GBK）② 五六个 `.bat` 共用同一段探测逻辑，
改一处不会漏四处 ③ 生成时自动校验「内容纯 ASCII」。

**规矩**：`.bat` 里**永远不出现绝对路径**。
需要路径就用 `%~dp0`（脚本自身所在目录）。

### 19. 单文件 exe 的启动黑框 / 报错看不见

`console=False` 打包出来的 exe 双击后**没有任何输出窗口**，
好处是不弹黑框，代价是**Python 的报错完全看不见** ——
程序闪一下就没了，不知道错在哪。

**排查办法**：临时把 spec 里的 `console=False` 改成 `console=True`，
重新打包。运行时会弹一个黑框，traceback 就显示在那里。

**另一个办法**（不用重新打包）：程序里自己把关键信息写成日志文件。
本项目在打包自检时就是这么做的（写 `selftest.log` 到 exe 同级），
比盯着黑框看更可靠。

**注意**：`console=False` 时 `print()` 不是报错，只是输出被丢掉了。
所以代码里那些 `print` 在打包版里等于不存在 ——
真要给用户看的信息得走 `pygame` 画到屏幕上。

### 20. 环境探测写文件又删文件，会把整个程序弄死 ⚠️

这条是打包完之后才暴露出来的，而且**非常隐蔽**，值得单独记一笔。

`game_env.py` 最早判断「这个目录能不能写」的办法是**写一个探针文件再删掉**：

```python
def _writable(d):
    try:
        probe = d / ".write_probe"
        probe.write_text("x")     # 写
        probe.unlink()            # 删 ← 问题在这
        return True
    except OSError:               # ← 只拦了 OSError
        return False
```

看起来天衣无缝，实际有两个问题叠在一起：

**问题一：删文件抛的不是 `OSError`，而是 `SystemExit`。**
运行环境里有一层「删除保护」，删文件删多了（本回合累计超过 50 个）会直接
抛 `SystemExit(1)`。而 `SystemExit` 继承自 **`BaseException`**，不是
`Exception` 的子类 —— 所以 `except OSError` **根本拦不住它**。
结果就是：探针一写、一删、整个 Python 进程当场退出。

**问题二：这个探测在 import 阶段就被调用。**
`describe_environment()` → `user_data_path()` → `_writable()`，
只要有人 import `game_env` 就会跑。于是五个测试脚本、以及游戏本身，
全部**在 import 那行静默死掉**，屏幕上什么报错都没有
（打包版连黑框都没有，更难查）。

**症状**：明明代码没错，一运行就秒退，exit code = 1，没有任何 traceback。

**修法**（两条都要做）：

```python
# 1) 不用「写+删」探针，改用静态权限判断，不产生任何文件
def _writable(d):
    try:
        if not d.is_dir():
            return False
        return os.access(str(d), os.W_OK)
    except (OSError, ValueError):
        return False

# 2) 兜底要 catch BaseException（含 SystemExit / KeyboardInterrupt），
#    环境探测出任何意外都不能让游戏起不来
try:
    if _writable(primary):
        return primary.joinpath(*parts)
except BaseException:
    pass
return (Path(os.environ.get("APPDATA") or Path.home()) / "数与形").joinpath(*parts)
```

**由此得出一条通用规矩**：

> 凡是「为了探测环境」而做的小动作（写文件、删文件、起子进程、读注册表），
> 外面一律包 `except BaseException`。这类代码的定位是**尽力而为**，
> 失败时的正确行为是**安静降级**，绝不是把整个程序带崩。

顺带一个好处：改用 `os.access` 之后，exe 同级目录里不会再冒出
`.write_probe` 这种垃圾文件了（以前每跑一次都会留下 / 删掉一个）。

### 21. 滚轮 / 方向键写反了：别在事件层直接传正负数

用户反馈「鼠标滚轮的逻辑反了」。一看确实是反的，但**根因不是符号写错**，
而是**在事件层直接操作了一个方向不直观的量**。

链条是这样的：

```python
# map_scene.py
def world_to_screen(self, wx, wy):
    return int(WIDTH/2 + wx), int(HEIGHT/2 + (wy - self.cam_y))
```

`cam_y` **变大** → 所有内容的屏幕 y 变小 → 内容整体**上移** →
我们看到的是塔的**下方**。

也就是说：

| 操作 | 应该 | 原来写的 |
|---|---|---|
| 滚轮上滚 / `↑` | 往塔的**上方**看 → `cam_y` **变小** | `scroll(+y)` → cam_y 变大，反了 |
| 滚轮下滚 / `↓` | 往塔的**下方**看 → `cam_y` **变大** | `scroll(-y)`，也反了 |

**修法不是把符号翻过来**（那样下次还会写反），而是给滚动画两个
**语义化的名字**，把方向约定收口在一处：

```python
def scroll_up(self, amount):
    """视野往塔的上方走（能看到更高的楼层）。"""
    self.scroll(-amount)

def scroll_down(self, amount):
    """视野往塔的下方走（能看到更低的楼层）。"""
    self.scroll(amount)
```

调用方从此只写 `scroll_up(70)` / `scroll_down(70)`，
**不需要也不应该再自己推 cam_y 的正负**。

> **教训**：凡是「正方向不直观」的量（相机、坐标变换、缩放、旋转），
> 都别让调用方直接传带符号的数字。包一层语义化方法，
> 把「哪个方向对应哪个符号」这个知识点**只写一遍**。
> 这个 bug 之所以能活到现在，就是因为有三处调用各写各的 `scroll(±n)`，
> 每处都得重新推一次。

**验证手法**：这种 bug 在**事件层**，所以不能只调 `scroll_up()` 自证
（那只能证明方法对，证明不了接线对）。必须**投真事件**进主循环：

```python
wheel(g, 1)          # 投一个真的 MOUSEWHEEL y=+1
assert g.map.cam_y < y0     # 往上看 -> cam_y 变小
```

`verify_scroll_deck.py` 第 1 组就是干这个的，滚轮和 `↑↓` 四条线全覆盖。

### 22. 条件断言会让测试「看起来全绿」，其实没测到 ⚠️

修完上一轮的代码，顺手跑回归，结果发现 `verify_save` 的通过项数
**在 52 和 54 之间飘**。FAIL 一直是 0，所以肉眼看着完全正常。

原因在这一段：

```python
n = first_reachable(g)
if n:
    g.enter_node(n)
    if g.mode == "battle":          # ← 靠运气
        check("战斗中拒绝存档", ...)
        check("拒绝时有提示", ...)
```

地图是**每局现生成的**，贪心走一步不一定踩到战斗节点。
实测跑 8 次，**只有 1 次**真的走进战斗 —— 也就是说
「战斗中不能存档」这条规则有 **7/8 的概率根本没被验证**。

**这比测试失败更危险**：失败至少会报警，这个只会安静地少测两条。

**修法**：不要靠「走一步看看能不能碰到」，**直接把状态摆出来**：

```python
g.mode = "battle"
g.battle = BattleScene(g.player, enemy_kind="battle")
check("战斗模式下拒绝存档", g.save_game(quiet=True) is False)
check("非战斗模式下可以存档", g.save_game(quiet=True) is True)   # 反例！
```

第二条反例很重要：只断言「战斗中一定拒绝」，一个「永远拒绝存档」的
错误实现也能通过。加上反例才算真的验证了条件分支。

修完连跑 6 次，稳定 55 项。

> **两条规矩**：
> 1. **别在测试里写「如果碰到了就测」**。要么把状态摆成确定的样子，
>    要么跳过并**打印一句说明**（至少能看见漏了什么）。
> 2. **同一个坑我在同一轮里又犯了一次** —— 新写的
>    `verify_scroll_deck.py` 里「进节点模式」那条也是假设
>    「第一个可达节点不是战斗」，随机失败了一次。
>    修法是 `nd["type"] = "rest"` 把类型钉死。
>    **凡是依赖随机生成物的断言，都要显式指定它依赖的那个属性。**
>
> 顺手加的一个自检习惯：**跑完回归，把各脚本的通过项数记下来对比**。
> 项数变化（哪怕 FAIL=0）就是有断言没跑到，值得追。

### 23. 给滚动测试补断言，顺手挖出一个「滚到顶就空屏」的真 bug ⚠️

第 21 条把滚轮方向收口成 `scroll_up()` / `scroll_down()` 之后，
这轮我给它们补了几个方向断言。写断言时想顺手验一下「滚到顶会停在哪儿」，
一打印就发现不对劲：

```
滚到顶  cam=-1768 | 屏内 1/12 行   最高行屏幕y=720（贴在最下沿）
滚到底  cam= 216  | 屏内 2/12 行
```

**滚到顶只剩 1 行在屏幕里，而且那行还卡在屏幕最下边。**
根因在 `clamp_camera` 的边界符号推反了：

```python
# 旧写法（错）
top = row_nodes[-1][0]["wy"]        # 顶层，wy 最小
bot = row_nodes[0][0]["wy"]         # 底层，wy 最大
lo = top - HEIGHT * 0.5             # ← 注释写「能看到顶层」，实际把顶层推到屏幕下沿
hi = bot + HEIGHT * 0.30            # ← 同理，方向反
```

为什么容易推反：`wy = -(row * ROW_H)`，**往上爬 wy 越来越小**，
而屏幕公式是 `画面y = HEIGHT/2 + (wy - cam_y)`，
所以 `cam_y` 变**大** = 内容整体上移 = 看到塔的**下方**。
「往上看」和 `cam_y` 的正方向是相反的，`lo` / `hi` 谁配哪一端特别容易记混。

**修法：不要另写一套边界公式，直接复用 `camera_for()`**——
它就是「走到那一行时镜头该停的位置」，拿它当上下界，
等于保证「无论怎么滚，镜头落点都和正常行走时的构图一致」：

```python
lo = self.camera_for(self.row_nodes[-1][0])   # 顶层构图
hi = self.camera_for(self.row_nodes[0][0])    # 底层构图
```

**顺手验了一下修复效果**（`tmp/render_cam_extremes.py` 渲染两张图肉眼确认）：

| | 旧边界 | 新边界 |
|---|---|---|
| 滚到顶可见行数 | 1 / 12 | **4 / 12** |
| 滚到底可见行数 | 2 / 12 | **3 / 12** |

又扫了 300 张随机地图，确认「两端都不少于 3 行」是硬几何性质、不是运气：

> 行数恒为 12，可滚跨度恒为 1408，滚到顶最少 4 行、滚到底最少 3 行。

**同一个函数里还有个只写错注释、没写错代码的地方**：
`camera_for()` 的注释说「让节点落在 anchor 比例处 → 节点在下部」，
但把公式代进去化简是 `画面y = HEIGHT * (1 - anchor)`，
`anchor=0.62` 实际落在 **y≈274（屏幕上部）**，和注释说的正好相反。

**这里我选择只改注释、不改代码**，因为改成注释描述的构图，
虽然起点更饱满，但爬到顶层时 boss 会沉到屏幕下半部、上面空出一大片，反而更差。
现在的构图是两端折中的最优解。不过为了防止后人「照着错注释改符号」，
我把真实关系写成断言钉住了：

```python
for a in (0.30, 0.50, 0.62, 0.90):
    check("camera_for(anchor=%.2f) -> 节点在 y=%d" % (a, MS.HEIGHT * (1 - a)), ...)
```

> **两条经验**：
> 1. **注释和代码对不上时，先算一遍再说谁是错的**。这次是注释错、代码对，
>    但上一处是代码错 —— 只看一眼「好像注释写得挺明白」就照着改，会踩雷。
> 2. **边界值要打印出来看，别只断言「相等」**。如果我只写
>    `assert cam == 预期值` 而没打印「屏内几行」，
>    这个 bug 会一直躲在「钳制生效了」的绿灯下面。
>    属性断言（**看得到 3 行以上**）比数值断言（cam 等于某个数）更能抓住真问题。

---

### 24. 「exe 是新的吗」不能靠 grep，也不能靠时间戳 ⚠️

第 23 条修完相机边界、重新打完包，我顺手想确认「新代码真的进 exe 了」，
结果连踩三个坑 —— 三个坑的共同点是**都给出了看起来很有道理的假信号**。

**坑一：拿字符串裸 grep exe，永远搜不到。**

```python
data = Path("dist/NumbersAndForms.exe").read_bytes()
print("上下界直接复用" in data)      # False —— 但代码明明在里面
```

onefile 的 exe 里，PYZ 是 **zlib 压缩**过的，中文字符串当然搜不到。
我当时没意识到这点，第一反应是「是不是没打进去」，差点去重打包。
幸好我多问了一句「**这个方法本身可靠吗**」，于是拿几个
**百分之百一定存在**的字符串去试：

| 字符串 | 来源 | 裸 grep 结果 |
|---|---|---|
| `让节点落在屏幕` | `camera_for` 的旧 docstring | 无 |
| `限制相机范围` | `clamp_camera` 的旧 docstring | 无 |
| `演算者` | 普通字符串常量，游戏里满屏都是 | **无** |

连「演算者」都搜不到 —— 那问题就不在代码，在方法。

> **经验：一个检测手段在「已知为真」的样本上都报假，就先别信它的任何结论。**
> 我当时给自己的判据是：**先跑对照组**。对照组全灭 = 工具坏了，
> 不是被测对象坏了。这条现在写进了 `打包.py` 的 `BASELINE`。

**正确做法**：用 PyInstaller 自己的 reader 把 PYZ 解出来，看模块的字节码常量
（docstring 也会进 `co_consts`，所以能当指纹）：

```python
ar = CArchiveReader(str(exe))
raw = ar.extract([n for n in ar.toc if n.endswith(".pyz")][0])
tmp.write_bytes(raw[1] if isinstance(raw, tuple) else raw)
z = ZlibArchiveReader(str(tmp))
text = "\n".join(walk_strings(z.extract("map_scene")))
```

> 注意 spec 里 `optimize` 必须为 `0`。开了 `-OO` docstring 会被剥掉，指纹全失效。

**坑二：指纹必须「绑定模块」，而且必须是**从源码复制**的，不是回忆出来的。**

我第一版指纹表长这样（只有两元组，没有模块名）：

```python
FINGERPRINTS = [
    ("相机边界修复", "上下界直接复用"),
    ("camera_for 注释更正", "别再照着它改符号"),
    ("牌组面板", "按类型排序"),          # ← 两个错误叠在一起
]
```

而检查时只解了一个模块（`--module`，默认 `map_scene`）。于是：

1. 「牌组面板」这条**永远** FAIL —— 它的字符串在 `deck_view` 里，
   却拿去 `map_scene` 里找。结构上就不可能命中。
2. 更离谱的是「按类型排序」这个字符串**源码里根本不存在**，是我凭印象编的。

两条叠一起，输出是「3 条里挂了 1 条，exe 不是最新代码」——
**结论完全是错的，而它看上去特别像真的**。真去重打包只会白忙一场。

> **经验**：指纹字符串要 `grep` 出来再粘进去，不要手打。
> 而且每条指纹都得带上自己的模块名 —— 否则「搜错地方」和「代码没打进去」
> 会输出同一个 FAIL，没法区分。

**坑三：负向对照不能随便挑一个「看起来旧」的文件。**

为了证明检查器真能区分新旧，我拿 `release/_old_数与形_stale_0916113321.old/` 里的
exe 当"旧样本"。它**4 条指纹全过** —— 看起来像检查器没用。
但一看时间戳就明白了：那个目录是**一分钟前**一次失败运行留下的，
里面的 exe 恰恰是**新代码**。它根本就不是旧样本。

真正的旧样本得从**修复之前**打的那个 zip 里取（`_old_数与形-公理塔.zip`，11:04）：

| exe 来源 | 相机修复的两条指纹 | 牌组 / 血条两条指纹 |
|---|---|---|
| 11:04 的 zip（修复之前） | **FAIL, FAIL** | OK, OK |
| 现在 release / zip 里的 | OK, OK | OK, OK |

漏掉的正好是**这一轮新加的东西**，命中的正好是**上一轮就有的东西** ——
和时间线完全吻合。这才叫对照成立。

> **经验**：验证「检测器有效」的样本，必须是**你确知它旧**的样本。
> 「名字里带 old」「时间戳比较早」都不算，「符合预期地失败一次」才算。

现在这三条都以第 5 步的形式进了 `打包.py`，每次打包自动跑，
对不上就直接中止（不会安静地发出去一个旧 exe）：

```
[5/5] 内容自检：核对 exe 里的代码指纹
  对照组（必须命中，确认解析流程正常）：
    [OK  ] 解析流程基线                       限制相机范围
  代码指纹（必须命中，确认是最新代码）：
    [OK  ] 相机边界复用 camera_for            上下界直接复用
    [OK  ] camera_for 注释更正              别再照着它改符号
    [OK  ] 牌组面板高度自适应                    按卡片数量算面板高度并居中
    [OK  ] 血条随血量变色                      颜色随剩余比例变
  OK：4 条指纹全部命中，exe 里就是最新代码
```

以后每轮改完游戏代码，从新的 docstring 里挑一句加进 `FINGERPRINTS` 就行。

---

### 25. 改按键语义，先把测试里对旧语义的断言找出来 ⚠️

给 S / L 加「选存档槽」时，把 `handle_map` 里的行为从
「按 S 直接存档」改成「按 S 返回 `save_menu` 信号，由主循环弹槽位列表」。
一跑回归：`verify_keys` **7 项 FAIL** —— 全是钉着旧语义的断言
（「按 S 之后存档文件出现了」「按 L 返回了换局指令」…）。

这不是代码写错了，是**测试描述的交互已经不存在了**。但这 7 个 FAIL
值得停一秒：它们证明这些测试**当初真的测到了行为**，而不是摆设。
修复方式不是删断言，而是**把断言改写成新语义的两段式**：

```python
# 按键只负责「发出意图」
r = press(g, pygame.K_s)
check("按 S 返回「选槽存档」信号", r == "save_menu")
check("按 S 不直接写文件（要等选了槽才写）", not MGR.exists())
# 选槽之后才真正落盘（模拟主循环的后续动作）
g.save_game(slot=0)
check("选槽后存档文件出现了", MGR.exists())
```

→ **规矩：改任何按键 / 交互的语义之前，先 grep 测试目录里这个键的旧断言。**
→ 另一个坑同轮踩到：测试里 `monkeypatch save_system.SAVE_DIR` 之后，
   又混用 `SaveManager(path=自定义)` 和 `SaveManager(slot=0)` 两种构造，
   两者指向**不同文件**，断言 `exists()` 互相打架。
   一轮测试里只用一种构造方式。

---

### 26. 「画出来给人看」的东西不会报错，只能靠像素断言 ⚠️

这一轮给卡牌和敌人加图案（全是代码画的几何图形）。两个"不报错、
但一眼就丢人"的错误，都是**像素断言**抓出来的：

**错误一：敌人浮在半空。** 原来敌人是个红圆，圆心按 `E_CY = 435 - E_R`
算 —— 圆底落在 y=435，而地面线（玩家脚底、脚下影子）在 y=486，
敌人整体浮在地上方 57px。红圆时代看不出来（圆本来就是个抽象符号），
换成有底有脚的几何形象后立刻露馅。

断言要问的是「用户看到的是否对」，而不是「代码是否按我写的算」：

```python
bottom = bbox(surf)[3] - cy                    # 形象最低那个像素离中心多远
check("底边落在脚下", 0.80 * r <= bottom <= 1.06 * r, ...)
```

**错误二：三个圆盘摆成"正品字"，重心偏上。** 精英敌人的排布角度写的是
`-pi/2`（第一个圆在正上方），实测底边只到 0.73r，看着像飘着。
改成 `+pi/2`（第一个圆落在正下方当底座）后底边到 0.88r，才"站得住"。
—— 这个如果只靠肉眼看截图，很可能觉得"还行"就放过去了。

**同类风险：图案画到卡外面 / 所有卡画成同一个图。** 复制粘贴新卡图案时
最容易漏改。用同一套像素手法钉住：

```python
mask = (pygame.surfarray.array3d(surf) != 255).any(axis=2)  # 非白像素矩阵
inked = mask.sum()                       # 「画了没有」
ys, xs = np.nonzero(mask)                # 「画到哪了」（包围盒）
raw = pygame.image.tobytes(surf, "RGB")  # 「两张卡是不是同一个图」（比字节）
```

→ **规矩：任何"画出来给人看"的东西（图案、图标、布局），断言要落在像素上。**
   「函数被调用了」「没抛异常」都证明不了画对了。
→ 计数和包围盒用 numpy 一遍算完。逐像素 `get_at` 在 400×400 上要 16 万次
   调用，慢到没法用。

**顺带两条**：
- 图案按「可用方形边长」的比例绘制（不写死像素），所以同一张卡在战斗大卡
  （132 宽）和牌组小卡（104 宽）上形状完全一致，只是缩放不同。
- 没配图案的卡名必须走**兜底**（问号圆盘），不能静默留白 —— 否则以后加了
  新卡忘了配图，卡面一片白，还会被当成布局 bug 去查。

---

### 27. 给背景加坐标系：先量「空档」在哪，再决定图纸摆哪 ⚠️

背景里原来有两条**悬空漂着的裸曲线**（正弦、抛物线），看着像随手画的。
这一轮给它们配上真正的坐标系（带箭头的轴、刻度、单位网格、原点 `O`、
轴名、图注）。踩到的几个点：

**一是"有地方摆"不等于"有地方看得见"。** 一开始按「中央 x340~940 是空的」
就把图纸摆上去了，结果正弦的左翼（x368~444）整个压在玩家立绘下面 ——
立绘画布是 388px 宽（444×320 的帧缩到高 280），实际有像素的位置一直铺到 x444。
所以边界不能用"大概"的，得**量**：

```python
# 三个角色 × 全部动作帧，把每帧非透明包围盒映射回屏幕坐标，取并集
bb = frame.get_bounding_rect()
L = min(L, P_X + (bb.left - fw / 2.0) * s)   # s = 280 / fh
# → 实测并集 x56~444 / y206~486，这条断言直接写进 verify_bg_decor.py
```

最后能摆图纸的只有 **x448~886 / y126~274** 这一条（右边还要给 Boss 留到 x900、
中间要让开 x=640 的对决区中线）。

**二是"曲线在动"要用整条波来验，不能钉一列看。** 第一版测试固定一列
取曲线高度，跑半周期只有 35px 变化（理论 68px），看着像"没动够"——
其实那一列正好采在节点附近，`sin(x−φ)` 在节点处对 φ 最不敏感。
两个修法一起用：
- 固定列必须**跑满一个周期**，否则可能正好采在节点上；
- 更本质的是验「整条波右移了多少」：把两个时刻的曲线高度剖面做
  **互相关**，最佳平移量应等于 `unit × Δt / 620`（实测误差 < 1px）。

```python
s, err = best_shift(sine_profile(0), sine_profile(620))
check(abs(s - B.SIN_UNIT * 620 / 620) <= 3, ...)   # 19px，实测 19px
```

**三是装饰的坐标要提成常量，测试才能跟代码用同一份真相。**
漂浮符号、公式、几何图形原来都是方法里的局部字面量，测试只能把坐标
抄一遍 —— 改代码忘了改测试，就会"测试说没事、画面已经糊了"。
现在提到 `DECO_SYMBOLS` / `DECO_FORMULAS` / `DECO_SHAPES`，
符号的漂移公式也提成 `deco_symbol_offset(i, t_ms)`，
于是测试能把**整个漂移过程**扫一遍（每 50ms 取一帧），
而不是只检查一个静帧"当前恰好没压到"。

**四是裁剪。** 曲线用 `screen.set_clip(图纸矩形)` 包住，跑出框的线头会被切掉。
一定要记得**存旧裁剪再还原**（`old_clip = screen.get_clip()`），
否则后面所有绘制都会被莫名其妙地裁掉 —— 这类 bug 的现场离原因很远，
所以断言里专门有一条：扫全屏，曲线色像素必须 100% 落在图纸内。

---

### 28. 「文案说了、代码没做」是一类独立的 bug ⚠️

玩家报的三件事里有两条是同一个病根：**面向玩家的效果文案早就写好挂在界面上了，
代码里一处都没实现**。翻出来的清单：

| 文案写在 | 原话 | 当时的实际情况 |
|---|---|---|
| `tower.yaml` 精英节点 | 阶段化敌人，**掉落遗物** | `ENEMY_KINDS["elite"]` 里只有一个 `gold` |
| 代价「开区间」 | 下一节点必为精英，**战后额外获得 1 次强化** | 只锁了精英，强化那半没写 |
| 代价「负债增量」 | **获得 1 件随机遗物**，但本层所有敌人血量 +15% | 只加了 15% 敌血 |
| 三个角色的 `trait` | （选角卡面底部那一行） | 纯文字，战斗里没有任何分支 |

最恶劣的是「负债增量」：罚的那半（敌人 +15% 血）实实在在生效，奖的那半没有 ——
玩家选了它等于纯亏。**一罚一奖的代价，两边都得落地。**

修完顺手立了一条自检习惯：**每写一条面向玩家的效果文案，
就立刻去代码里指出它在哪一行结算。指不出来，就是还没做。**

### 29. 格挡「只有显示、没有效果」：清算顺序错了 ⚠️

`end_turn()` 里有一行 `self.p_block = 0`，看着很合理（「回合结束了，格挡清零」），
但它排在 `phase = "enemy"` **之前** —— 敌人还没出手，格挡已经没了，
于是实际伤害永远是全额，而格挡徽章照常画在脚边。

规则本身没错（格挡确实不跨回合），错的是**落点**：
该在**新回合开始**清，不是本回合结束时清。
现在清零点挪到 `enemy_act()` 末尾（也就是新回合开头），
`end_turn()` 只负责把「点结束那一刻的格挡」记进 `carry_block` 给【承形】用。

> 这类 bug 有个共同特征：**只断言「状态对不对」是抓不到的。**
> 光调 `resolve_card()` 看 `p_block == 6` 完全正常 ——
> 必须走完整的 `end_turn() -> enemy_act()`，比**血量差分**才暴露。

### 30. 同一个文件不要并行发多个编辑 ⚠️

这轮有几次「明明提示写入成功，回头 grep 却找不到」——
原因是同一批里对**同一个文件**发了多个编辑：各自读旧内容、再各自写回，
后写的把先写的覆盖了。丢的偏偏是 `import`、函数定义这类，
症状是运行时报 `NameError`，而现场离原因很远。

**规矩**：同一个文件的改动一律串行；要批量改就写成一整个 Write。
改完顺手 grep 一遍关键符号（函数名、import），确认真的落盘了。

### 31. 界面写着「可以组合」，代码里只存了一句提示 ⚠️

玩家点数字卡，界面立刻提示「已选数字 3，可再点图形卡组合」——
而代码里那一句只是 `self.combo_hint = "..."`，**没有任何分支处理"第二次点击"**：
再点图形卡走的是"改选另一张卡"那条老路，组合永远不会发生。
玩家看到的是一句**承诺**，不是一个功能。

这和第 28 条是同一个病根，但形态更隐蔽：第 28 条那句文案挂在 YAML / 角色卡上，
这一句是**运行时现写的提示**，写提示的人和写逻辑的都以为对方做了。

规矩加一条：**提示语里出现的动作词（「再点…可以…」「按 X 可以…」），
必须能在 `handle()` 里指出对应分支。指不出来 —— 要么把它实现，要么删掉这句话。**

同轮顺手清掉一个同类隐患：`art_shapes` 查图案 / 查形状时用的是卡名原样，
而强化会给卡名加 `+`（「三角盾+」）—— 强化过的卡图案全掉成问号圆盘。
现在查表前统一过 `base_card_name()` 剥后缀。

### 32. 「单张卡的费用」和「组合的费用」不是一回事 ⚠️

早先选牌时就查能量：`if self.p_energy < c.cost: return`（选都选不了）。
组合改成「两张卡之和 −1 优惠」之后，这条检查就变成了**拦路虎**：
能量只剩 2 点时，平方卡单张 2 费能选中，可要是把它调到 3 费，
玩家连"选中"都做不到 —— 尽管「数字 1 费 + 平方 3 费 − 优惠 = 3 费」
本来是他付得起的一次组合。

**规矩**：费用检查只在**真正出牌的那一刻**做（单出看单张费用，
组合看 `combo_cost()`）。选中一张牌只是个"拿起"的动作，不该收钱。

### 33. 牌组一变大，选卡网格就掉出屏幕 ⚠️

数字牌 0~9 一来，牌组从 10 张变成 16~17 张，休整点的强化选卡变成三行 ——
三行 × (176 + 14) 从 y=176 排下来，最后一行底边到了 **y=732**，
整个掉在 720 之外：看得见一半，点不到。

而且这种布局当时写了**三份**（休整点 `ensure_card_layout`、战利品
`card_grid_rects`、商店 `_layout_remove_cards`），各写各的折行 ——
改一份不动另外两份，症状就是「有的界面正常、有的界面出屏」。

**规矩**：布局收口到 `fit_grid_rects()` 一份实现，它保证**整个网格落在
指定纵向区间里**（行数一多就按比例缩小卡片，宽高同步缩不变形），
并且写测试盯着 1~60 张都不出屏。另外卡片自身的绘制也不要写死 32 / 52
这类绝对像素 —— 缩过的卡会被字画花，按 `r.h` 算比例。

### 34. 题目别用字符串拼，用表达式树 ⚠️

算术题从「两位数加法」升级成「四则 + 括号」时，最容易出的错不是不会算，
而是**题面和答案对不上**：拼字符串的时候括号漏了一个、优先级搞反一处，
玩家按题面算出来的答案就被判错 —— 而且这种错很难靠肉眼发现。

做法：先造一棵表达式树 `(运算符, 左, 右)`，**题面**（`_expr_text`，按优先级
自动补括号）和**答案**（`_eval_expr`，递归求值）都从同一棵树来，
结构上不可能对不上。除法也靠"先定商和除数、再相乘得被除数"构造出来，
保证整除；不在算完之后做「不合规就重掷」的兜底（那种兜底会死循环）。

测试对应地也不信代码里的 `ans`：把题面文字里的 `×` `÷` 换成 Python 的
`*` `//`，**直接 eval 那句话**独立复算（`verify_number_quiz.py` ②）——
题面写错一个字就会露馅。

---

### 35. 音效名写错不会报错，只会「静悄悄没声音」 ⚠️

`sfx.play("hit_lite")` 拼错一个字母：不抛异常、不报错、日志干净，
只是**那个场合没有声音**。等你自己发现「好像打中没声」，可能已经过了好几天。

这类 bug 的性质和「画出来给人看的东西不报错」是同一个（见第 26 条）——
只能靠测试兜住。做法是**从源码里扫名字再逐个核对**：

```python
# verify_sfx.py
for path in HERE.glob("*.py"):
    if path.name == "sfx.py":      # 定义文件自己不算使用方
        continue
    used |= called_names(path.read_text(encoding="utf-8"))
missing = [n for n in used if n not in sfx._SPECS]
```

扫描本身有个坑：不能「一行一条正则」。有两类写法会漏或误报 ——

```python
# ① 跨行调用：只匹配一行的正则抓不到后面的参数
sfx.play("hit_heavy",
         gap_ms=0)

# ② 参数区里混进了非音效名的字符串
#    "area" / "square" 是**题型**，不是音效
sfx.play("combo_ok" if kind in ("area", "square") else "answer_ok", gap_ms=0)
```

所以扫描要自己配平括号取参数区，再用一个**显式**的排除名单
（`_NON_SOUND_LITERALS = {"area", "square", "arith", "name"}`）。
显式列出来而不是把规则写得更聪明 —— 规则一复杂，真出现拼错的音效名时
反而可能被漏掉。

反方向也顺手查了：「定义了却在整个代码库里一次都没出现」= 死代码。
但这里**不能**只认「直接写在 `play(...)` 里」，有些名字是绕一层到达的：

- `hit_light/mid/heavy/massive` 写在 `battle_scene.DMG_TIERS` 表里
- `bgm_battle/bgm_explore` 在 `_sync_bgm` 里赋给 `track` 再传进 `play_bgm`

只认直接调用的话，这七条会被整体误报成「没人用」。

---

### 36. 1.4 秒的合成不能堵在启动路上 ⚠️

34 条音效全部算完要 **1.4 秒**（实测）。如果放在 `sfx.init()` 里同步做，
双击 exe 之后要黑屏卡 1.4 秒才出菜单 —— 这是「第一印象」级别的伤害，
而它本来完全可以不发生。

做法：**算 PCM 交给后台线程，建 `pygame.mixer.Sound` 回主线程。**

| 步骤 | 在哪做 | 为什么 |
|---|---|---|
| 算波形 → 归一化 → 转 16 位字节 | 后台线程 | 纯计算，不碰 SDL，慢也没关系 |
| `pygame.mixer.Sound(buffer=...)` | **主线程** | 要碰混音器，不在线程里做更安全 |
| 收结果 | 主线程每帧 `_drain()` | 只从 queue 取（名字 + bytes），**共享状态为零** |

于是 `init()` 从 1459ms 降到 **70ms**，音效算好一个就能用一个。
合成顺序按「玩家最先听到的」排：UI 音最先、BGM 放最后
（BGM 要 1 秒才算出，但主循环每帧都会重试起播，晚一秒进音乐完全无感）。

顺带两个必须注意的点：

1. **`Sound(buffer=...)` 不复制数据**（SDL_mixer 的 `Mix_QuickLoad_RAW` 是
   「包一层」）。传进去的 `bytes` 一旦被回收，声音就变成随机噪声甚至崩掉。
   所以 `_PCM_KEEP` 里显式留着一份引用不放 —— 总共才几 MB，不值得省。
2. **归一化之后不用逐个样本钳位**。`|v * g| <= peak <= 1.0` 是构造保证的，
   于是最热的那段转换循环可以省掉每样本两次比较。

---

### 37. 延时反馈要用游戏时间，不能用墙钟 ⚠️

「敌人倒地 → 0.5 秒后号角 → 1.05 秒后金币」这种排队靠 `play_after()`。
它最早是用 `time.perf_counter()` 记「到期时刻」的，**这是错的**：
游戏里所有别的东西（BGM 淡入淡出、角色动画、敌人回合倒计时）都走
主循环喂进来的 `dt`，只有它走墙钟。两种时间轴混在一起的后果是
**游戏卡一下，画面还停在原处，声音已经抢跑了**；被暂停时更明显。

改成拿 `dt` 累减之后，它和别的系统自然同步，而且测试里可以精确控制：

```python
sfx.play_after("win", 0.15)
sfx.update(0.05)          # 还没到
assert sfx.count() - c == 0
sfx.update(0.20)          # 到了
assert sfx.count() - c == 1
```

用墙钟的话这个断言根本没法写（得真的 sleep）。

---

### 38. 测「瞬态」别用「峰值 / 整体 RMS」⚠️

想用数据证明打击音「起手就猛」，第一版写的是

```python
crest = peak(xs) / rms(xs)        # 期望 > 2.5
```

结果**最有打击感的两条反而被判失败**：`hit_heavy` 2.4、`hit_massive` 1.9
（轻击 6.1 通过）。原因是重击和巨击专门做了**长余震**
（0.3~0.4 秒的隆隆声），长尾巴把整体 RMS 抬上去，指标反而变小。

换成「**起手能量 / 收尾能量**」就对了：

```python
def front_ratio(xs):
    return rms(xs[:len(xs) // 6]) / rms(xs[len(xs) // 2:])
```

它直接对应直觉里的「前猛后散」，而且不被尾巴长度干扰。另外还加了一条
**对比断言**：持续型的音（回血）不该是「前猛后散」——
绝对阈值容易被调参弄失效，和反例对比才稳。

同一个毛病在「上行琶音」那条检查上也犯过：最早用整段前后半的过零率比，
`answer_ok` 被判成 0.30（明明是上行）。因为音效**末尾是已经衰减到听不见
的一段**，那一段的过零率是浮点噪声，把它当「音高」自然全错。
正确做法是先掐掉首尾静音，再用 Goertzel 量「八度差两个频点」的能量比。

### 39. 面板加了新阶段，测试驱动里的 if/elif 会把它吞掉 ⚠️⚠️

这个坑**踩了第二次**（第一次见第 27 条），所以单开一条记清楚。

战利品面板原本只有「遗物 -> 强化」两段，smoke 的驱动函数写的是：

```python
if p.stage == "relic":          # ← 只认这一个阶段
    g.handle_node(...)
while not p.done and p.card_rects and guard < 6:
    ...
```

后来面板前面插了一个「解锁新卡（平方）」的 `unlock` 阶段，
这个函数就再也走不完面板了。**症状离原因非常远**：
「能爬到第 2 层」失败 —— 面板不结束 -> `after_node()` 不跑 ->
楼层永远不推进。查的时候一路在怀疑地图 / 换层逻辑。

修法不是补一个 `elif p.stage == "unlock"`，而是**去掉阶段枚举**：

```python
while not p.done and guard < 12:
    if p.stage in ("unlock", "relic"):
        pos = p.btn_take.center      # 「收下」型阶段
    elif p.card_rects:
        pos = p.card_rects[0].center # 「选一张」型阶段
    else:
        break
    g.handle_node(_ev(pos), pos)
```

> 教训：**驱动一个多阶段 UI 时，不要按「当前有哪几个阶段」写分支** ——
> 那是把当时的状态抄进测试。要按「这个阶段需要什么操作」来分。

### 40. 「本层」类效果必须在换层时归位 ⚠️

路线代价「负债增量」的文案是「**本层**所有敌人血量 +15%」，
但 `MapScene.enemy_hp_mult` 只在 `__init__` 里初始化过一次，
`load_floor()` 里**没有归位**。于是：

- 罚的那半（敌人 +15% 血）从选它的那一层开始，**永久**跟着玩家爬到塔顶
- 奖的那半（1 件遗物）只兑现一次

越往后越是纯亏，而且玩家完全看不出原因（没有任何界面显示这个倍率）。
修法就是 `load_floor()` 开头一句 `self.enemy_hp_mult = 1.0`
（读档时 `main._init_from_save` 会用存档里的值覆盖回来，顺序刚好对）。

> 立规矩：凡是文案里带「本层 / 本回合 / 本次」的效果，
> 都要问一句「那它在什么时候复位？」—— 而且复位点要写在同一个模块里。

### 41. `to_dict` 写了、`from_dict` 没读 = 字段静默丢失 ⚠️

`Player.to_dict()` 一直在写 `pending_upgrades`，但 `from_dict()` 里
**一行都没有读它**。后果：路线代价「开区间」承诺的「战后额外获得 1 次强化」，
存个档再读回来就凭空消失了。类里没有任何报错，两边都能正常跑。

这轮顺手修了，并且加了个约束：**`to_dict` 里出现的键，`from_dict` 必须都有对应行**。
新加的两个字段（`removals_done` / `square_unlocked`）都按这个规矩办了，
而且都用 `.get(...)` 兜默认值 —— 老存档没这两个键，读回来是 `0` / `False`。

同一件事还有两个变体，一起记：

- **老存档自带平方**（那时平方还在初始牌组里）：`from_dict` 末尾会检查
  「牌库里有平方吗」，有就补上 `square_unlocked = True` ——
  否则读档后商店永远不卖平方，玩家会当成 bug
- **老存档内嵌的 `map_data`** 没有这轮新加的 `enemy_scale` / `boss_theme` /
  `boss_hp`：`main.enemy_kwargs()` 一律用 `.get()`，缺了就退回 1.0 / None，
  一进战斗不会 `KeyError`

### 42. 难度倍率只允许在一处相乘 ⚠️

加「越往上越难」时，最省事的写法是在 `BattleScene.__init__` 里
`hp * scale`、在 `roll_intent()` 里再给攻击乘一次。**这条路一定会漏**：
以后再加第三个受倍率影响的数值（格挡？强化？），没人会记得回来补。

所以把倍率收口到一个纯函数 `battle_scene.scaled_enemy()`，
它一次算出 `{hp, atk, block, buff}`，构造函数和出招都只读结果：

```python
def scaled_enemy(cfg, hp_mult=1.0, scale=1.0, hp=None):
    ...
    return {"name":..., "hp":..., "atk": atk, "block":..., "buff":...}
```

两个细节：

- `hp` 参数非空时**不再乘 scale** —— 层主血量是逐层单独配的
  （见 `tower.yaml`: `boss_hp`），再乘一遍等于扣两次难度
- 返回的 `atk` 是**倍率不是伤害**：普通攻击每次在 `ENEMY_ATK_RANGE` 里随机，
  倍率要乘在那次随机的结果上。以前那个 `randint(7, 12)` 直接写在
  `roll_intent` 里，根本没法被倍率影响

> 生命吃满额、攻击只吃一半（`ENEMY_ATK_GENTLE = 0.5`）也是刻意的：
> 玩家没有稳定的回血手段，攻击跟着满额涨会把「难」变成「运气不好就死」。

### 43. 同一份清单抄两处，早晚会漂移 ⚠️

「打包指纹」这张表原本有两份：`打包.py` 里一份，`tmp/verify_exe_contents.py`
里另抄一份。抄的时候两边都是 4 条，看起来无伤大雅；等 `打包.py` 那张表
涨到 44 条，手动核对脚本里还是 4 条 —— 于是**两边报告都写「全部命中」，
但检查的根本不是同一批东西**，手动那次等于白查。

现在 `verify_exe_contents.py` 直接 `import 打包` 取 `PKG.FINGERPRINTS`，
全项目只有一张表。

同类问题还有一处：`tmp/verify_fingerprints.py`（打包**前**扫源码）
曾经只按 `根目录/mod.py` 找模块，而 spec 的 `pathex` 里还有
`map_tools/tools`。`build_map` 的指纹于是被静默判成「[SKIP] 没有这个文件」——
预检说「没有」、exe 里其实「有」。现在它的搜索路径与 spec 的 `pathex` 一致，
并且**找不到源码直接算 FAIL**（那说明模块名写错了，或模块搬了家）。

> 一条通用判据：凡是「A 处和 B 处各写了一遍同一件事」，就要问一句
> 「它们会不会有一天不一样」。会 —— 就改成引用，或者删掉一份。

### 44. 折行文字的高度要算出来，不能写死 y ⚠️

`draw_wrapped()` 本来就是按 `max_w` 折行的，但**只有它知道会折几行**。
排版时如果靠肉眼估一个 y 写死，中文一长就必然压到一起 ——
「新卡到手」那段说明就被「收下」按钮盖掉了下半截（截图里第一眼就能看到）。

修法是把折行逻辑抽成 `wrapped_lines()`，再做两个薄封装：

```python
def wrapped_height(text, font, max_w):
    return len(wrapped_lines(text, font, max_w)) * (font.get_height() + 3)
```

然后布局**先量后摆**：算出内容占多高 → 决定框多高 → 按钮放在内容下方
（`SpoilsPanel._layout_unlock()`）。这样以后文案加长也不会再压在一起。

两个配套细节：

- **提示语要提成常量**（`UNLOCK_HINT`）。文案写在 `draw` 里、布局在
  `__init__` 里量，两边就会各写一遍同一句话 —— 又是第 43 条那个病。
- **按钮矩形按阶段取**：`btn_take` 做成 property（新卡段 / 遗物段两个框）。
  存成两个字段就一定会有人读错那一个，症状是「按钮看得见、点了没反应」。
  另外测试点 `panel.btn_take.center` 这种方式能继续用，不会因为拆分而点空。

> 光断言「面板画得出来」是抓不到压字的 —— 必须把按钮框和文字块
> 两个矩形求交，断言为空（见 `verify_floor_progress.py`）。

### 45. 能从已有数据推出来的量，就别再存一份

同样是「单一来源」，但这次栽在**运行时状态**上。平方层主写「n²」的文案
需要基数 n。第一版为了图省事存了个 `e_square_n`，结果被测试里
「直接设 `e_intent = "square"`」的路径绕了过去，意图框上出现
**「平方 0²=16」**——伤害是 16，标签写着 0²。

`n²` 本来就存在 `e_intent_val` 里，所以正确的做法是**开方还原**：

```python
def _square_n(self):
    return int(math.isqrt(max(0, int(self.e_intent_val))))
```

少一个字段，也就少一处能不一致的地方。

> 有意思的是这个 bug 是**测试输出**里看出来的（`[OK] ... 平方 0²=16`）——
> 断言只查了「16 在文本里」，所以它当时是「通过」的。
> 教训：断言要查到**关键的那个数**（`"%d²" % n in txt`），而不是随便一个数。

---

### 46. 入口脚本不在 PYZ 里 —— 只查 PYZ 会漏掉所有指向 `main` 的指纹 ⚠️⚠️

**这个坑躺了很久，因为它在「本机能跑」的时候永远不暴露。**

本机预检（`tmp/verify_fingerprints.py`）是读**源码**拼字符串，
它当然能找到 `main.py` 里的每一句话 —— 所以它一直全绿。
可是 `打包.py` 第 5 步读的是**打完包之后的 exe**，那里的世界完全不一样。

装上 PyInstaller 第一次真跑打包，第 5 步就报：

```
!! exe 里不是最新代码，缺 2 条指纹：F1/F2 开关音效与音乐、战利品里真的发了平方
```

而这两条指向的都是**同一个模块 `main`**，其余 46 条全过 ——
这个「挂的全是同一个模块」的模式，就是本坑的指纹。

**根因**（用 reader 开包实测）：

```python
ar = CArchiveReader(str(exe))
z  = ZlibArchiveReader(pyz_path)
len(z.toc)              # 232 个模块
"main"     in z.toc     # False   ← 入口脚本根本不在 PYZ 里
"__main__" in z.toc     # False
"main"     in ar.toc    # True    ← 在这一层
```

PyInstaller 把**被 import 的模块**塞进 PYZ（zlib 压缩），
但**入口脚本是单独处理的**：它以 **marshal 过的 code object**
直接躺在 CArchive 里，条目名就是脚本名（`main.py` → `"main"`，
头 4 字节 `b'c\x00\x00\x00'` 正是 marshal 的 code 标记）。

于是「只查 PYZ」的自检对 `main` 的指纹一律判 FAIL，
汇总成一句**看起来特别像真的**结论：「exe 不是最新代码」。
照着它去重打包、去怀疑增量缓存，全是白忙。

**修法：查两个地方。** 而且这份逻辑**只写一遍**
（第 43 条刚讲过「同一份清单抄两处早晚漂移」，这次是同一个病的复发）——
收口成 `打包.py` 的 `open_exe_strings()`，
`打包.py` 第 5 步和 `tmp/verify_exe_contents.py` 共用：

```python
if mod in z.toc:                       # ① 被 import 的模块
    text = "\n".join(walk_strings(z.extract(mod)))
elif mod in ar.toc:                    # ② 入口脚本，marshal 的 code object
    data = ar.extract(mod)
    if isinstance(data, tuple):
        data = data[1]
    for blob in (data, data[16:]):     # 带 pyc 头时跳过 16 字节
        try:
            text = "\n".join(walk_strings(marshal.loads(blob))); break
        except Exception:
            continue
```

顺带把报错分开说：**「模块找不到」和「字符串找不到」不是一回事**——
合成同一个 FAIL 就没法区分「指纹写错了模块名」和「打包真的漏了代码」。

> 一条通用判据（和第 43 条同源）：**凡是「同一件事有两份实现」，
> 就要问「它们会不会有一天不一样」。** 会 —— 就收口成一份。
> 这次是「读取 exe 的逻辑」，上次是「指纹表」，下次可能是别的。

**还有一处连坐**：`tmp/_run.py`（通用 runner）里写了 `int(e.code or 0)`，
而 `打包.py` 自检失败时抛的是**字符串** `SystemExit`，
`int()` 直接 ValueError —— 这个异常发生在 `except` 块里，
于是**写输出文件那行代码根本执行不到**，我最想看的那份指纹明细
（48 条逐条结果）全丢了，只剩一个 ValueError traceback。
修好后才知道「挂的是 main 那两条」这个关键线索。

> 所以：`except SystemExit as e` 之后**不能假设 `e.code` 是整数**。
> 凡是「环境不具备就中止」的脚本都爱用字符串抛异常。

### 47. 在「盖在别的界面上的面板」上加按钮：点击必须比节点判定先问它

给地图左侧面板加「遗物 / 查找」两个按钮时踩到的：
面板和地图**叠在同一块屏幕上**，`handle_map` 的顺序原本是
「先判鼠标底下有没有节点」。加按钮之后如果不改顺序，
点「遗物」按钮会**顺手把按钮底下的节点也点进去** —— 玩家想查个遗物，
人却往旁边走了一格。而且这种 bug 很隐蔽：按钮生效了、图鉴也打开了，
只有「人莫名走了一格」这一条不对。

正确顺序：

```python
if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1:
    if self.map.handle_side_click(mouse):   # ① 先问面板上的按钮
        return None
    nd = self.map.hovered_node(mouse)       # ② 再判节点
    ...
```

> 同一类问题还有一层：**覆盖层（牌组 / 遗物图鉴）开着时，键要先给它。**
> 尤其是 `ESC` —— 有面板时 `ESC` 只能关面板，不能顺手把游戏退掉。
> 这个判断放在 `Game.handle()` 最前面（和 `deck_view` 那条并排），
> 不要塞进 `handle_map`：`handle_map` 前面还有 `D` / `M` 两个全局键，
> 漏掉哪个都会在「面板开着」时干出第二件事。
>
> 对应测试：`verify_relic_find.py` 里专门断言了
> 「点按钮不会让人走一格」「图鉴开着时 ESC 不返回 quit」。

### 48. 面板上有可点的东西，布局就不能在 `draw` 里现推

第 5 条（布局写在 `draw` 里 → 第一帧点不到）的延伸版，这次是**另一种翻车方式**：
布局本身在 `draw` 里推没问题，但**点击命中也得推一遍**，
两遍只要有一点不一样，就变成「按下去的地方不一定是画出来的地方」。

而且这块面板还**会变高**：多一行「有存档」或「未完待证」就长 22px。
画的时候 `has_save` 是 `main` 递进来的，点的时候如果自己去调一次
`has_save()`（读盘判断），两次结果就可能不同。

所以抽了 `map_scene.player_panel_layout(has_save, hide_next)`：
**矩形一次算清，画和点都来取这一份**。
`main` 每帧把判断结果塞进 `map.has_save_hint`，两边读的是同一个值。

> 断言方式：把 `has_save_hint` 打开 -> 面板变高 -> 点按钮**仍然命中**；
> 再关掉 -> 点同一位置也命中。布局变而命中跟着变，才算真的共用了一份坐标。

### 49. 顺手收掉的三份「同一段折行逻辑」

做遗物图鉴时又需要按像素折行，翻出来发现已经有**三份**：
`deck_view.wrap_text`、`node_scenes.wrapped_lines`、
以及各处 `draw` 里现写的一小段循环。这次收口到 `game_env.wrap_text`，
另外两处改成一行转发。

顺带修掉一个藏在里面的小毛病：老版本对「**单个字就超宽**」的输入会先
`append("")` 再放那个字，折出来的第一行是个空行 —— 排版按行数算高度时
就会莫名多出一行。新实现加了 `elif cur:` 分支挡掉这种情况。

> 判据还是第 43 / 46 条那句：同一件事有两份实现，就问它们会不会不一样。

### 50. 指纹不能挑「写在 `#` 注释里」的句子 ⚠️（第二次踩）

打完包自检报了两条落空：

```
[FAIL] 地图能查找遗物来源      哪些节点会出遗物
[FAIL] 侧栏按钮优先判于节点    先问左侧面板上的按钮
```

这两句在源码里**明明都有**（我刚写的），本地预检也一起报落空 ——
因为指纹是在**字节码常量**里找的，而 Python 编译时**注释直接被丢掉**：

| 文字写在哪 | 进不进 `co_consts` | 能不能当指纹 |
|---|---|---|
| 模块 / 函数 docstring | ✅ 进 | ✅ |
| 普通字符串字面量（提示语、日志、报错） | ✅ 进 | ✅ |
| 变量名 / 函数名 | ✅ 进（`co_names`），但 `walk_strings` 不收 | ❌ |
| **`#` 注释** | ❌ **不进** | ❌ |

修法很简单：换一条挨着它的**字符串字面量**。
`哪些节点会出遗物` → 换成 tooltip 文案 `遗物来源 · %s：%s`；
`先问左侧面板上的按钮` → 换成 `查找遗物：开`。

> 现在 `打包.py` 的指纹表上方已经把这条钉成注释了：
> **「指纹要从『会进字节码的文字』里挑。」**
> 这个坑上一轮（加音效指纹时）就踩过一次，这次原样复发 ——
> 说明「写在某个文件里的注意事项」挡不住下一次，
> 得写在**每次都要打开的那个文件**里。

---

## 十二、接下来打算做的模块

按优先级排：

- [x] ~~**地图 ⇄ 战斗打通**~~ — 已完成：走到 `×` 战斗节点直接进战斗，打完回地图
- [x] ~~**节点内容实现**~~ — 已完成：休整点 / 商店 / 事件 / 宝箱四种面板全部可交互，效果真实生效
- [x] ~~**存档 / 读档**~~ — 已完成：`S` 存 / `L` 读，换层自动存，种子复现地图（详见第七章）
- [x] ~~**地图随机性**~~ — 已完成：每格独立掷骰子 + 轻量约束，60 轮抽样零扎堆（详见第十章）
- [x] ~~**节点每次运行都不同**~~ — 已完成：地图改为运行时现场生成（`build_in_memory`），开局必出新图
- [x] ~~**开场界面 + 选角色**~~ — 已完成：主菜单 / 三张角色卡 / 载入画面，动的是占位方框（详见第三章）
- [x] ~~**打成免安装 exe 发给别人**~~ — 已完成：`打包.py` 一键出 26MB 单文件 exe，
      打包后存档/字体/资源路径全部自动适配（详见第八章）
- [x] ~~**战斗血条**~~ — 已完成：血量改成血条画在角色头像正下方，
      数字压进条里，颜色随剩余比例绿→琥珀→红（详见第五章末）
- [x] ~~**查看自己的牌组**~~ — 已完成：任意场景按 `D` 弹出 `deck_view.DeckView`，
      按类型排序、自适应高度、牌多可滚动（详见第六章）
- [x] ~~**鼠标滚轮方向反了**~~ — 已完成：收口成 `scroll_up()` / `scroll_down()`，
      方向和按键全对齐（详见第十一章第 21 条）
- [x] ~~**滚到顶 / 滚到底只剩一两行（空屏）**~~ — 已完成：`clamp_camera()` 的上下界
      改为复用 `camera_for()`，两端各能稳定看到 4 / 3 行（详见第十一章第 23 条）
- [x] ~~**验证「发出去的 exe 装的是新代码」**~~ — 已完成：`打包.py` 第 5 步读 PYZ
      核对代码指纹，对不上就中止打包（详见第十一章第 24 条）
- [x] ~~**多存档槽**~~ — 已完成：3 个槽位 + 槽位选择界面，
      旧 `save.json` 自动迁移成槽 1（详见第七章）
- [x] ~~**游戏内回主菜单**~~ — 已完成：地图 / 节点 / 战斗按 `M` 都能回
      （原来只有死亡画面能回，中途只能退出整个程序）
- [x] ~~**三个角色都有立绘**~~ — 已完成：三套立绘 + 待机/攻击/受击动画
      全部接入（`char_art.py`，素材预处理见 `tmp/prepare_char_art.py`）
- [x] ~~**卡牌图案与敌人形象**~~ — 已完成：`art_shapes.py` 纯代码几何图案，
      数字牌 0~9 + 各形 / 运算卡各有母题、3 档敌人各有形象（详见第五章）
- [x] ~~**背景里的函数配上坐标系**~~ — 已完成：两个带刻度/箭头的坐标系，
      y=sin x 横向流动、y=x² 开口呼吸，曲线裁在图纸内（详见第五章）
- [x] ~~**数形结合组合出牌**~~ — 已完成：数字卡 + 图形卡组合出**面积题**
      （两张卡同时生效），图形卡单出考**认图形名称**（详见第五章）
- [x] ~~**数字牌 0~9 + 难度阶梯**~~ — 已完成：十张数字牌，伤害＝牌面数字，
      难度从「一位数加法」一路升到「带括号的四则」；题面由表达式树生成，
      答案用同一棵树算（不可能对不上）（详见第五章）
- [x] ~~**平方卡改成组合牌**~~ — 已完成：运算卡，不能单出，
      配数字卡出「先看例题、再算 n²」的题，伤害＝数字的平方（9 → 81）
- [x] ~~**组合成功打出省 1 点能量**~~ — 已完成：两张卡费用之和 −1（最低 1 点）
- [x] ~~**层主战与楼层推进**~~ — 已完成：最上面那行的 `★` 打完后 `next_floor()` 进下一层。
      三层各有自己的层主（名字/血量/难度倍率写在 `tower.yaml`）；
      第一层层主「正方体·三阶」是平方主题（蓄力 n → 打出 n²），
      **打赢它才解锁「平方」卡**（详见第四章）
- [x] ~~**三层难度递增**~~ — 已完成：`tower.yaml` 每层的 `enemy_scale`
      = 1.00 / 1.45 / 1.90，生命吃满额、攻击吃一半（`ENEMY_ATK_GENTLE`）
- [x] ~~**商店删牌要有限制**~~ — 已完成：一次商店只能删一张，
      且每删一张下次更贵（60 → 90 → 120 …）
- [x] ~~**平方卡改成层主掉落**~~ — 已完成：三人起手都没有，
      解锁前商店也不卖；打赢第一层层主后进牌库（`player.unlock_square`）
- [x] ~~**三个角色的被动接进战斗**~~ — 已完成：三条被动都在 `battle_scene.py` 里结算，
      战斗日志会写明哪条生效、生效多少（详见第四章）
  - 演算者【直感】首张数字卡 +1 / 构形师【承形】余留格挡换下回合 +2 / 解方程者【代入】每回合多抽 1 张
- [x] ~~**精英 / 层主战利品**~~ — 已完成：打完整套流程发遗物 + 选卡强化（`node_scenes.SpoilsPanel`）；
      第一层层主那一场还多一段「新卡到手（平方）」
- [x] ~~**在地图面查找遗物**~~ — 已完成：左侧「遗物 N 件」点开**遗物图鉴**
      （拿到的高亮、没拿到的灰着、底部写来源）；`R` 键或左侧按钮开**查找**，
      把会出遗物的节点加环标注、其余压暗，顶部横幅给本层统计（详见第二章）
- [ ] **敌人血量继续按平方量级细调**（9 号牌 + 平方一发 81 点；
      第 2/3 层已经按 `enemy_scale` 加过血，但第一层之后的手感还要玩过才知道）
- [ ] **卡牌数据表**（把卡从 `player.py` 硬编码搬进 `map_tools/data/cards.yaml`）
- [ ] **角色数据表**（把 `CHARACTERS` 也搬进 `data/characters.yaml`，现在写死在代码里）
- [ ] **战斗核心抽离**（伤害公式独立成模块，支持**无头跑几千局**测平衡）
- [ ] **数学题库继续扩**（算术题现在按 6 档难度出，还缺知识点维度：
      平方开方 → 方程 → 面积体积）
  - 图形题这边已经落地：面积题 6 种图形（真实比例 + 尺寸标注）、认图形四选一
  - 平方题已经带上「例题 → 本题」的结构，可以照这个样式再做「开方 / 方程」
- [ ] **敌人配置表**（把 `ENEMY_KINDS` 也变成 YAML 数据）
- [ ] **更多节点事件**（现在只有 3 个事件，重复进入会撞到同一个）
- [x] ~~**音效与动效**~~ — 已完成：`sfx.py` 全程代码合成 34 种音效 + 2 首循环 BGM
      （零音频文件，后台线程合成不卡启动），打击感按伤害分四档，
      配套震屏 / 闪白 / 飘字，`F1`/`F2` 随时开关（详见第五章末）
- [ ] **音效继续打磨**（现在音量是保守调的，没有真机听过；
      另外可以让 BGM 随血线变化、给「快赢了」加一段变奏）

---

## 十三、策划案

完整策划案在：`C:\Users\sanji\Desktop\肉鸽卡牌游戏策划案.docx`

包含 12 章正文 + 4 个附录，其中负面效果已全部用数学概念重新命名
（误差累积 / 约等损耗 / 失根 / 收敛极限 / 分段函数 / 不等号反转 /
取近似 / 开区间 / 未完待证 / 负债增量 等）。
