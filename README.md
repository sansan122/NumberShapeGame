# 数与形 · NumberShapeGame

一个把**数学知识点**变成卡牌机制的肉鸽卡牌游戏原型。

---

## 一、怎么运行（最重要）

在 `C:\Users\sanji\Desktop\游戏制作尝试\` 里，**双击 `0_START_HERE.bat`**，然后按提示输入数字回车：

```
[1] 公理塔地图       ← 主游戏，爬塔选路线
[2] 卡牌战斗         ← 战斗演示
[3] 重新生成地图     ← 固定种子，每次同一张
[4] 随机生成地图     ← 每次都是新地图
[5] 我的第一个窗口   ← 最早那个红方块窗口
[0] 退出
```

也可以直接双击对应的单独脚本：

| 想做什么 | 双击这个 |
|---|---|
| 玩地图（主游戏） | `3_run_game.bat` |
| 只看地图 | `2_run_map.bat` |
| 玩卡牌战斗 | `1_run_card.bat` |
| 重新生成地图（固定） | `map_tools\2_gen_map.bat` |
| **随机生成地图** | `map_tools\3_gen_map_random.bat` |

> ⚠️ **不要修改 `.bat` 文件里的中文，也不要用记事本另存为 UTF-8。**
> 这几个 `.bat` 是特意做成"纯英文内容 + GBK 编码"的。
> 原因见下面「避坑记录」。

---

## 二、地图怎么玩

这是参考《杀戮尖塔》的爬塔地图：**起点在最下面，一路往上爬到层主**。

打开 `3_run_game.bat` 后是一个 1280×720 的窗口：

| 操作 | 效果 |
|---|---|
| **鼠标滚轮** / `↑` `↓` | 上下滚动视角（镜头跟随） |
| **点击高亮的节点** | 移动过去，进入那个节点 |
| **点击远处的节点** | 不移动，而是高亮出「从当前位置到它」的完整路径 |
| `S` | **存档**（存到 `saves/save.json`） |
| `L` | **读档**（回到存档点） |
| `R` | 重开本层 |
| `ESC` | 退出 |

**哪些节点能走？** 只有**下一行**的节点，它们会带一圈**蓝色呼吸光晕**。
其余节点是暗的，点了只会给你看路径。

### 节点图标对照

图标用的是**数学符号**，和《数与形》的主题呼应：

| 图标 | 类型 | 说明 |
|---|---|---|
| `×` | 普通战斗 | 常规敌人，掉落卡牌与形值 |
| `◆` | 精英战斗 | 阶段化敌人，掉落遗物 |
| `=` | 休整点 | 回复生命或强化一张卡 |
| `√` | 商店 | 用金币买卡牌、遗物，或移除卡牌 |
| `?` | 事件 | 三选一抉择，带世界观文本 |
| `∑` | 宝箱 | 直接获得一件遗物 |
| `★` | 层主 | 本层最终战 |

### 路线代价（本作的差异化机制）

连线上如果有个**金色小圆圈**，说明这条边带「路线代价」——走上去要付出代价，
但代价的名字全部来自数学概念，让惩罚本身也变成一次记忆强化：

| 代价 | 效果 | 数学出处 |
|---|---|---|
| **取近似** | 跳过 2 个节点，但失去 8 点最大生命 | 四舍五入求省事，必然丢精度 |
| **开区间** | 下一个节点必为精英，但战后额外获得 1 次强化 | 开区间端点取不到 |
| **未完待证** | 目标节点类型不可见 | 条件未证，结论未知 |
| **负债增量** | 获得 1 件随机遗物，但本层所有敌人血量 +15% | 借来的量要连本带利还 |

把鼠标悬停在带代号的连线上，可以看到这个代价的完整说明和数学出处。

---

## 三、点击节点之后会发生什么

走到节点上，会按类型自动进入对应的内容：

| 节点 | 进去之后 |
|---|---|
| `×` 普通战斗 | 打「几何魔像」（50 血，胜了给 28 金币） |
| `◆` 精英战斗 | 打「方程组·三元」（78 血，给 55 金币） |
| `★` 层主 | 打「不可解之影」（120 血，给 120 金币） |
| `=` 休整点 | 二选一：**回复生命**（30% 上限）或**强化一张卡**（数值 +3） |
| `√` 商店 | 3 张随机卡 + 1 件遗物。另有「花 60 金币移除一张卡」的服务 |
| `?` 事件 | 随机抽一个事件，每个事件有 3 个选项，**选择会真的改变玩家状态** |
| `∑` 宝箱 | 点开箱子，**直接获得一件随机遗物** |

### 休整点

进去是两个大按钮：

- **回复生命** — 回满 30% 最大生命（80 上限就是回 24 点），点完立刻离开
- **强化一张卡** — 进入牌库网格，点哪张就强化哪张（伤害/格挡 +3，抽牌 +1），名字会带个 `+`
  - 左下角「返回」可以退回菜单改主意

### 商店

四件商品一字排开，价格按卡牌费用算（`40 + 费用 × 20`）：

- 买的卡**直接进你的牌库**，本局后续战斗都能抽到
- 遗物买下**立即生效**
- 「**移除一张卡**」服务：付 60 金币，从牌库永久删掉一张（用来精简牌组）
- 金币不够会提示「还差多少」，不会误扣

### 事件

目前有 3 个事件，每个 3 个选项，都是**真实生效**的：

| 事件 | 选项 |
|---|---|
| **断裂的等式** | 补上缺口（-10 生命，得 1 件遗物）/ 绕过去（无事发生）/ 铭记它（最大生命 +8） |
| **未完成的证明** | 替它写完（+40 金币，最大生命 -6）/ 撕掉它（移除牌库一张卡）/ 折起收好（+25 金币） |
| **沉默的演算者** | 与它交换（-20 金币，最大生命 +10）/ 点头走过（+15 金币）/ … |

选完再点一下画面任意处即离开。

### 遗物一览

| 遗物 | 效果 |
|---|---|
| **勾股定理** | 战斗内**首次**打出图形卡，额外 +3 格挡 |
| **质数筛** | 战斗开始时，从牌库移除 1 张最弱的卡 |
| **换元法** | 每回合**第一次算错不消耗卡牌**（卡留着） |
| **对数尺** | 每回合多抽 1 张牌 |
| **约等号** | 所有伤害 +1 |
| **公理石** | 最大生命 +12 |

---

## 四、卡牌战斗怎么玩

打开后是一个 1280×720 的浅色窗口：

1. **点手牌选一张卡**（卡片会浮起变黄）
2. **再点一下空白处**＝决定打出这张卡
3. 这时会弹出一道题，等式右边是一条**下划线**（填空线）
4. **直接敲数字键**，输入的数字会实时显示在下划线上，光标跟随闪烁
   - 按 `退格` 删掉最后一位
   - 按 `回车` 提交（或点「提交」按钮）
5. 点右下角 **「结束回合」** 轮到敌人行动

判定结果：
- ✅ **算对了**：卡牌效果生效
- ❌ **算错了**：这张卡**直接进弃牌堆**，白白浪费

**核心设计**：想打牌，先算对。数学不是外挂的答题小游戏，而是出牌的**前置门槛**。

### 手牌类型

| 卡 | 类型 | 效果 |
|---|---|---|
| 加一 | 数字 | 造成伤害 |
| 凑十 | 数字 | 造成伤害 |
| 平方 | 数字 | 造成伤害 |
| 三角盾 | 图形 | 获得格挡 |
| 方阵 | 图形 | 获得格挡 |
| 镜像 | 图形 | 抽牌 |
| 归零 | 图形 | 清空敌人格挡 |

### 界面元素

- 左上是「演算者」（你自己）：生命 / 能量 / **形值**
- 右上是敌人「几何魔像」：生命 / **意图**（下回合要干什么，提前告诉你）
- 右中是**战报**：每次操作的文字记录
- 下方是手牌区，左下角是操作提示

---

## 五、存档 / 读档

### 怎么用

| 什么时候 | 按什么 |
|---|---|
| 地图上想存一下 | `S` |
| 想回到上次存的地方 | `L` |
| 死了想回到存档点 | `L`（死亡画面上会提示） |
| 死了想从头再来 | `R`（会**顺手删掉存档**） |
| 打完层主自动换层 | **自动存一次**（这是最自然的存档点，不用你操心） |

存档放在 `saves/save.json`，玩家状态面板底部出现 **「有存档　L 读档」** 就说明有档。

### 存档里存了什么

```json
{
  "version": 1,                  // 格式版本，以后改结构用
  "saved_at": "2026-09-15 21:22:17",
  "seed": 485897898,             // ← 关键：地图的随机种子
  "floor_index": 1,              // 在第几层
  "current": 0,                  // 站在哪个节点
  "visited": [0],                // 这层走过哪些节点
  "player": { "max_hp": 80, "hp": 63, "gold": 175,
              "relics": [...], "deck": [...] },
  "map_effects": { "hide_next": true, "enemy_hp_mult": 1.32 },
  "floor_stats": {}
}
```

### 为什么地图不用整个存下来？

因为地图是「**种子 → 地图**」的确定性函数：**同一个种子跑出来的地图一模一样**。
所以存档只要记住 `seed`（一个数字），读档时重新生成一遍就行 ——
存档文件因此只有 **2~3 KB**，而不是几十 KB。

万一种子对不上（比如你中途换了张地图），程序会**降级**处理：
只恢复玩家状态 + 楼层 + 站位，并在地图战报里记一句
「读档：地图已重新生成，只恢复了状态」。**血和牌不会丢。**

### 容错设计（都有测试覆盖）

| 情况 | 表现 |
|---|---|
| 存档不存在，按 `L` | 提示「读档失败：没有存档」，不崩 |
| 存档 JSON 写坏了 | 提示「存档损坏」，原文件改名为 `save.json.bad` 留档（不直接删） |
| 存档版本比程序新 | 拒绝加载，提示版本号 |
| 存档字段残缺 | 缺的字段用默认值补上，能启动 |
| 血量是负数 / 超上限 | 自动夹回 `0 ~ max_hp` |
| 牌库里混了坏卡 | 跳过那张坏卡，保留其余 |
| 存档写到一半断电 | 先写 `.json.tmp` 再改名，原存档不受影响 |

### 已知限制

- **战斗中不能存档**（按 `S` 会提示「战斗中不能存档」）。
  战斗中场面状态太多（手牌、弃牌堆、敌人意图），完整序列化成本高，
  收益却低 —— 爬塔游戏的标准做法就是只在地图上存。
- 存档是**单槽**的，再存一次会覆盖上一份。

---

## 六、目录结构

```
游戏制作尝试/
├── 0_START_HERE.bat        ← 双击这个（菜单）
├── 3_run_game.bat            主游戏启动器（地图 → 节点 → 战斗 → 下一层）
├── main.py                 ★ 主程序入口（顶层状态机 map / node / battle / dead）
├── map_scene.py            ★ 地图场景（爬塔、选路线、路线代价）
├── node_scenes.py          ★ 非战斗节点面板（休整点 / 商店 / 事件 / 宝箱）
├── battle_scene.py         ★ 战斗场景（可被调用的战斗类）
├── player.py               ★ 跨场景共享的玩家状态（生命 / 金币 / 遗物 / 牌库）
├── save_system.py          ★ 存档 / 读档（SaveManager + 版本号 + 容错）
├── saves/                    存档目录（自动创建，已被 .gitignore 忽略）
│   └── save.json               存档文件（约 2~3 KB）
├── 2_run_map.bat             单独跑地图
├── 1_run_card.bat            单独跑战斗
├── test_card.py              独立版卡牌战斗（答题才出牌）
├── 4_run_my_test.bat         红方块窗口
├── my_test.py                最早的红方块窗口
├── README.md                 本文件
├── tmp/                      验证脚本与截图（已被 .gitignore 忽略）
│   ├── verify_nodes.py       六种节点全量验证（无头，63 项断言）
│   ├── verify_save.py        存档读写验证（无头，49 项断言）
│   ├── verify_keys.py        按键链路验证（S / L / ESC，28 项断言）
│   ├── smoke_game.py         端到端冒烟（21 项断言）
│   ├── render_shots.py       批量渲染各面板截图
│   └── render_save_shots.py  渲染存档相关界面截图
└── map_tools/                地图数据与生成工具
    ├── 2_gen_map.bat         重新生成地图
    ├── data/                 配置表（YAML）
    │   └── tower.yaml          三层塔配置（改这个来调平衡）
    ├── tools/
    │   ├── build_map.py        生成地图数据 → out/map.json
    │   │                       （--random 每次不同 / --seed N 可复现）
    │   ├── render_map.py       生成静态预览网页 → out/map.html
    │   └── sample_distribution.py  跑 40 轮抽样，检查分布是否合理
    ├── 3_gen_map_random.bat  随机生成地图（每次都不一样）
    ├── out/                  生成结果（自动创建）
    └── 使用说明.md            地图工具详细说明
```

**重要**：`map_scene.py` 读的是 `map_tools/out/map.json`。
想改地图，编辑 `map_tools/data/tower.yaml`，然后跑 `map_tools\2_gen_map.bat`
（固定图）或 `map_tools\3_gen_map_random.bat`（随机图），
再启动主游戏就会看到新地图。

---

## 七、地图工具

运行 `map_tools\2_gen_map.bat`，会：

1. 读取 `data/tower.yaml` 里的三层塔配置
2. 生成节点与连边 → `out/map.json`
3. 画成一张 SVG 网页 → `out/map.html`，并**自动用浏览器打开**

### 想要一张新地图？

有三种模式，**双击 `map_tools\3_gen_map_random.bat` 就是每次全新的地图**：

| 想做什么 | 怎么做 |
|---|---|
| **每次都不一样**（推荐） | 双击 `map_tools\3_gen_map_random.bat`，或命令行 `--random` |
| 固定那张调试用的图 | 双击 `map_tools\2_gen_map.bat`（默认种子 `20260915`） |
| 复现某张特定的图 | 命令行 `--seed 1234`，种子号会打印出来 |

生成时会打印**随机种子**，想再抽到同一张图就把它填给 `--seed`。

### 节点类型怎么撒的

这一节前后改过**三版**，把演进过程记下来，免得以后又绕回去。

**第 1 版：逐行独立掷骰子 + 每 5 行指定一个「特殊行」。**

结果是**每行几乎都是「3 个战斗 + 1 个事件」**，特殊行又变成清一色
（`=休整 √商店 √商店 √商店`）。根因是普通行的候选池排除了
`rest/shop/treasure` 但**漏了 `event`**，而 `event` 权重不低，
抽 4 个位置几乎必然中一个。

**第 2 版：先按权重算整层配额，再打散撒进各行。**

修掉了上面的 bug，但引入了新的「太整齐」问题：

- 每层节点数几乎一样（实测恒定 32/35/38），因为行数固定、
  `randint(2, width)` 的期望值恒定
- 每种特殊节点数量被配额钉死，每层都是「休整 2、商店 2、事件 2~3」
- 强制「每行最多 1 个特殊节点」，特殊节点像打格子一样均匀铺开

用户的原话是「**地图随机性不行，再改改**」—— 问题不是"缺项"，
而是**方差太小**，观感上觉得每次都一样。

**第 3 版（现在）：每格独立掷骰子 + 轻量约束。**

随机性交给概率自然生长，约束只在「明显不合理」时兜一下：

1. **每行几个节点：真的随机**（1~`width`，允许只有 1 个的
   「独木桥」行，那是很好的节奏变化）
2. **每个格子：从全部类型按 `weight` 掷骰子**（含特殊节点）
3. **软约束**：同一行不出现两个同类型特殊节点；
   相邻两行不连续出同一种特殊节点；一行最多 2 个特殊节点
4. **硬兜底**：掷完之后统计每种特殊节点的总数，
   低于下限就补、高于上限就换成普通战斗

另外还做了两处「手感修正」：

- **开局第一回合必须有得选**：起点行只有 1 个节点，它连到第二行
  是按 x 位置就近连。如果第二行只有 1 个节点，那「第一回合选路」
  这个动作就不存在了。所以强制第二行 `>= 2` 个节点，
  并且把最无聊的格子换掉 —— 一个换休整点、一个换商店，
  于是开局永远是「**稳一手 or 拼一把**」。
- **连边数量也随机**：1 / 2 / 3 条按 `40% / 45% / 15%` 掷，
  原来固定 2 条看起来很规整。

实测 60 轮抽样（`map_tools/tools/sample_distribution.py`）：

| 检查项 | 结果 |
|---|---|
| 地图唯一性 | 60 / 60 张互不相同 |
| 每层节点总数 | 20 ~ 43（标准差 4.08） |
| 精英战斗数量 | 0 ~ 12（标准差 2.05） |
| 商店数量 | 1 ~ 5（标准差 1.00） |
| 缺项 | 三层 × 四类型全部出现过 |
| 扎堆（一行 ≥3 个特殊节点） | 0 次 / 2520 行 |
| 死路 / 走不到 Boss | 0 个 / 0 张 |

对比第 2 版（节点数恒定 32/35/38、精英恒定 5/6/5），
现在每层的规模、精英密度、特殊节点位置都在自然波动。

当前输出（固定种子 `20260915`）：

| 层 | 名称 | 节点 | 连边 | 带代价的边 |
|---|---|---|---|---|
| 1 | 塔基 · 数之庭 | 21 | 27 | 5 |
| 2 | 塔身 · 形之廊 | 32 | 45 | 7 |
| 3 | 塔冠 · 方程之顶 | 33 | 49 | 7 |

（用随机模式每次数量和连边都会变。）

**改完记得重新生成地图**：`map.json` 是生成出来的静态文件，
改了 `build_map.py` 不重跑 `tools/build_map.py`，
游戏里看到的还是旧图。这个坑踩过一次（见避坑记录第 13 条）。

---

## 八、避坑记录（踩过的坑，别再踩）

### 1. `.bat` 报 `Code language not supported or defined`

**现象**：双击脚本，窗口一闪，报这行错。

**原因**：`cmd.exe` 按 **GBK** 解码 `.bat` 文件，但文件是 **UTF-8** 存的。
里面的中文变成乱码，乱码行里的 `chcp 65001` 也无法识别，于是报错。

**解决**（已在本项目全部改好）：
- `.bat` 内容**只用 ASCII 字符**（纯英文提示）
- `.bat` 文件用 **GBK 编码**保存
- **不写 `chcp 65001`**

这样在任何中文 Windows 上双击都正常，且不需要管理员权限。

> 如果以后要加中文提示，正确做法是用 GBK 编码写 `.bat`，
> 而不是改成 UTF-8 再 `chcp`。

### 2. 中文文件名的 `.bat` 引用

`测试.py` 已重命名为 `my_test.py`。
纯 ASCII 的 `.bat` 引用中文文件名会因编码不一致找不到文件，统一用英文名最稳。

### 3. 用哪个 Python

所有脚本用的是项目自带的隔离环境：

```
C:\Users\sanji\.workbuddy\binaries\python\envs\naf\Scripts\python.exe
```

已装好 `pygame 2.6.1` 和 `PyYAML 6.0.3`。
不用管系统的 Python，也不用额外装包。

### 4. `draw()` 撞名把自己覆盖掉（本轮踩的最狠的坑）

**现象**：`BattleScene` 一构造就报
`TypeError: BattleScene.draw() missing 2 required positional arguments`。

**原因**：类里有两个 `draw` ——

```python
def draw(self, n):                        # 抽 n 张牌
def draw(self, screen, mouse, t_ms):      # 画一帧
```

Python 后定义的**直接覆盖**前一个，于是 `self.draw(5)`（想抽牌）
调到了「画一帧」上，参数对不上。

**解决**：抽牌方法改名 `draw_cards()`。
**教训**：同一个类里别让「动作」和「渲染」共用 `draw` 这个名字，
渲染统一叫 `draw` / `draw_xxx`，其他动词换名字。

### 5. 布局写在 `draw` 里 → 第一帧点不到

**现象**：面板刚出现时点按钮没反应，得等一帧之后才正常。

**原因**：主循环是 `handle()` → `draw()` 的顺序。
`handle` 里要用 `self.btn_ok`，但它是**上一帧** `draw` 才创建的 ——
第一帧根本还不存在（或还是空的）。

**解决**：所有「点击判定要用到的矩形」都在 `__init__` 里算好；
如果矩形依赖运行时的状态（比如牌库变了），就在**改变状态的那一步**
同步重算，绝不放在 `draw` 里。

本项目已按这个原则改过 4 处：
`ShopPanel.item_rects` / `ShopPanel.card_rects` /
`RestPanel.card_rects` / `EventPanel.opt_rects` / `TreasurePanel.btn_ok`。

### 6. 打死敌人不结束战斗

**现象**：一刀把敌人砍到 0 血，战斗却没结束，要再点「结束回合」才判胜。

**原因**：`resolve_card()`（出牌结算）里忘了调 `check_end()`，
只有 `submit_quiz()` 和 `enemy_act()` 里调了。

**解决**：`resolve_card()` 末尾补上 `self.check_end()`。
**教训**：凡是会改动血量/胜负的入口，都要过一遍结束判定。

### 7. 卡片网格排到屏幕外

**现象**：休整点一进去，牌库网格的 x 从 `-119` 铺到 `1399`，左右都出屏了。

**原因**：布局用 `total = n * cw + (n-1) * gap` 居中，
10 张卡 × 132 宽早就超过 1280，居中的结果是**两边一起溢出**。

**解决**：改成**按行折行**（`per_row = (WIDTH - 120) // (cw + gap)`），
并且只在真正进入选卡界面时才排布。

### 8. 无头验证 pygame 的正确姿势

改完代码怎么确认没坏？**别靠肉眼看窗口**，用无头跑：

```python
os.environ["SDL_VIDEODRIVER"] = "dummy"   # 无显示器
os.environ["SDL_AUDIODRIVER"] = "dummy"

# 投递「真」事件（唯一能被 event.get() 消费的方式）
pygame.event.post(pygame.event.Event(
    pygame.MOUSEBUTTONDOWN, {"pos": (x, y), "button": 1}))
for ev in pygame.event.get():
    scene.handle(ev, (x, y))
```

- ❌ 假事件（`pygame.event.get = lambda: [...]`）**不会被消费**，
  连续三帧渲染结果一模一样，看着像"没 bug"，其实什么都没触发。
- ✅ 想检查「没崩」，直接调 `screen.fill()` + `scene.draw(screen, mouse, t)`；
  想检查「画面长什么样」，用 `pygame.image.save(screen, "x.png")` 存图再看。
- ⚠️ 项目里有 `tmp/verify_nodes.py`（63 项断言）和 `tmp/render_shots.py`（批量截图），
  改完节点/战斗代码建议都跑一遍。

### 9. 地图「每次都长一样」

**现象**：不管重跑多少次，地图永远是「3 个战斗 + 1 个事件」，一模一样。

**两个原因，要一起改**：

**(a) 种子写死了**
`build_map.py` 里 `SEED = 20260915` 是个常量，`random.Random(SEED)`
每次都从同一状态出发 → 逐字节相同的结果。
→ 改成 `--random` 时传 `None`（用系统随机源），并且**把实际用的种子打印出来**，
方便复现。

**(b) 类型分配是「逐行独立掷骰子」**
这是「3 战斗 + 1 事件」的真正来源：

```python
elif r % 5 == 4:                     # 每 5 行硬性一个特殊行
    ntype = rng.choice([...特殊节点...])
else:
    ntype = weighted_choice(         # ← 池子里把 rest/shop/treasure 排除了
        [t for t in node_types
         if t["id"] not in ("rest", "shop", "treasure")], rng)
```

普通池里只剩 `battle`(45) 和 `elite`(15) 和 `event`(12)……
等等，`event` **不在排除名单里**，所以权重 45:15:12 抽 4 个位置，
出来的就是「3 个 battle + 1 个 event」。
特殊行更惨：第一个位置定死是特殊节点，**剩下 3 个位置仍然从排除过特殊节点的池子里抽**，
于是变成 `=休整 √商店 √商店 √商店`（一行三家商店）。

**→ 改成「先规划整层配额，再打散撒进各行」**：先按权重算好每种要几个
（各带上下限、且浮动 ±1），把它们分到**不同行**（每行最多 1 个、
尽量互相隔开），剩下的位置才填普通/精英。

**顺带一个坑**：`boss` 不在 `tower.yaml` 的 `node_types` 里
（它是层主专属），所以 `type_by_id["boss"]` 会 `KeyError`。
→ 用 `.get()` 兜一个默认值。

### 10. 校验地图分布要「抽样」，不能只跑一次

只看一次生成结果，没法判断随机性够不够、有没有缺项。
`map_tools/tools/sample_distribution.py` 会跑 40 轮，检查四件事：

| 检查 | 期望 |
|---|---|
| 40 轮的地图是否互不相同 | 40/40 唯一 |
| 三层 × 四类型是否都出现过 | 零缺项 |
| 「一行 ≥3 个特殊节点」 | 0 次 |
| 每种特殊节点的数量区间 | 有波动 |

改完生成器建议跑一下，比肉眼看十来张图靠谱。

### 11. `handle()` 里忘记 `return`，信号全被吞掉

做存档时踩的坑，**症状很隐蔽**：按 `ESC` 不退出、按 `L` 不读档，
但程序不报错、界面也照常刷新 —— 看起来就像「按键没绑上」。

根因是顶层派发函数写成了这样：

```python
def handle(self, event, mouse):
    if self.mode == "map":
        self.handle_map(event, mouse)      # ← 返回值被丢了
    elif self.mode == "node":
        self.handle_node(event, mouse)
```

`handle_map()` 明明 `return "quit"` / `return ("replace", new_game)`，
但 `handle()` 没把它传出去，主循环永远收不到信号。

**→ 层层都要 `return`**。这条现在由 `tmp/verify_keys.py` 守着
（直接投递真 `S` / `L` / `ESC` 键，检查主循环拿到的返回值）。

**教训**：这类「返回值被吞」的 bug，看代码很难发现（缩进一样、
没有语法错误），**必须用测试从外部打进去**才能暴露。

### 12. 无头测试别用「真存档路径」

第一版 `verify_save.py` 里写 `g = M.Game()` 之后忘了替换 `save_mgr`，
结果测试跑完在项目根下留了个 `saves/save.json`；更早一版还在
`SaveManager.save()` 里硬编码了 `SAVE_DIR.mkdir()`，导致**即使用临时路径
也会凭空建出一个空的 `saves/` 目录**。

**→ 两条规矩**：
1. 测试一律用 `SaveManager(path=临时目录/xxx.json)`，别碰真存档
2. 建目录要建**「这份存档所在目录」**（`self.path.parent`），
   不是模块级那个常量 —— 否则自定义路径的存档会顺手污染默认位置

现在 `verify_save.py` 每次跑完临时目录都会被清掉，
项目根下不会多出任何东西。

### 13. 改了生成器不重跑，游戏里看到的还是旧地图

`map.json` 是 `tools/build_map.py` **生成出来的静态文件**，
游戏运行时只读它，不会现算。

所以「改完 `build_map.py` → 直接开游戏」看到的是**旧图**，
会误以为改动没生效。这次就被坑了一次：改完随机性跑测试全绿，
结果游戏里还是老样子。

**→ 规矩**：动过 `build_map.py` / `tower.yaml` / 任何 `data/` 里的配置，
必须重跑一次

```
map_tools/2_gen_map.bat
```

（等价于 `python map_tools/tools/build_map.py`）
再开游戏。

### 14. 「修好了」要用数据证明，还得盯住改动引入的新问题

本轮修「一行 ≥3 个特殊节点」时踩了连环坑，值得记一下：

**第一坑：以为改的是 A，其实 B 才是源头。**

我先把 `plan_floor_types()` 改了半天，扎堆数从 18 降到 0，
很满意地重跑其他验证 —— 结果一测「第一层能不能走到精英」，
发现 **0/30**：第一层的特殊节点全挤在开局三行，
后面全是战斗，走完整层遇不到几次。**修好了扎堆，修坏了节奏。**

**第二坑：真正制造扎堆的是另一段代码。**

后来写了个调试脚本（`tmp/debug_cluster.py`）分别统计
「`plan_floor_types` 的输出」和「最终节点」里的扎堆数，结论很干脆：

```
扎堆出现阶段统计：
  plan_floor_types 输出就有    0
  最终节点才有                 68
```

也就是说规划函数是干净的，扎堆是**后面 `generate_floor()` 里
那段「开局保证」代码**造的 —— 它只数了「战斗/精英够不够 2 个」，
没按整行宽度算预算，于是 `[事件, 宝箱, 战斗]` 这种行也被补成 4 个特殊节点。

**→ 三条规矩**：
1. **改动前先写个脚本把「哪一步产出的」钉死**，别凭猜改。
   分段统计（规划函数的输出 vs 最终节点）比读代码快得多。
2. **一处修完，另外几处必须全部重跑**。「60 轮抽样全绿」
   不能替代「三层都能走到精英」。
3. **加了新约束，就要回头检查旧代码有没有绕过它**。
   我加了「一行最多 2 个特殊节点」，但第 3 步的补位逻辑
   和开局保证逻辑都能绕过它 —— 补位那个还因为
   「一次挑够 N 个再一起改」导致计数没刷新，同一行被连补 2 个。
   改成**逐个补、每补一个重排一次候选**才对。

---

## 九、接下来打算做的模块

按优先级排：

- [x] ~~**地图 ⇄ 战斗打通**~~ — 已完成：走到 `×` 战斗节点直接进战斗，打完回地图
- [x] ~~**节点内容实现**~~ — 已完成：休整点 / 商店 / 事件 / 宝箱四种面板全部可交互，效果真实生效
- [x] ~~**存档 / 读档**~~ — 已完成：`S` 存 / `L` 读，换层自动存，种子复现地图（详见第五章）
- [x] ~~**地图随机性**~~ — 已完成：每格独立掷骰子 + 轻量约束，60 轮抽样零扎堆（详见第七章）
- [ ] **层主战与楼层推进**：打到最上面那行的 `★` 后，进入下一层（当前 `next_floor()` 已有骨架）
- [ ] **卡牌数据表**（把卡从 `player.py` 硬编码搬进 `map_tools/data/cards.yaml`）
- [ ] **战斗核心抽离**（伤害公式独立成模块，支持**无头跑几千局**测平衡）
- [ ] **数学题库**（按知识点分层：加减 → 乘除 → 平方开方 → 方程）
  - 当前题目只随机出「两位数加法」和「一位数乘法」，还太单薄
- [ ] **敌人配置表**（把 `ENEMY_KINDS` 也变成 YAML 数据）
- [ ] **多存档槽**（现在是单槽，再存就覆盖）
- [ ] **更多节点事件**（现在只有 3 个事件，重复进入会撞到同一个）
- [ ] **音效与动效**（出牌、算对/算错、受击的反馈）

---

## 十、策划案

完整策划案在：`C:\Users\sanji\Desktop\肉鸽卡牌游戏策划案.docx`

包含 12 章正文 + 4 个附录，其中负面效果已全部用数学概念重新命名
（误差累积 / 约等损耗 / 失根 / 收敛极限 / 分段函数 / 不等号反转 /
取近似 / 开区间 / 未完待证 / 负债增量 等）。
