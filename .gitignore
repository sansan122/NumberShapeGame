__pycache__/
*.pyc
map_tools/out/*.json
tmp/
saves/

# 音效开关（F1/F2）的本地记忆，属于用户数据；开发时落在项目根
audio.json

# PyInstaller 打包产物
build/
dist/
_SelfTest.spec
*.spec.bak

# 发布包（体积大，随时能用 打包.py 重新生成，不进版本库）
release/

# 环境探测探针（旧版 game_env 会临时写这个文件）
.write_probe

# 重打包时用来腾位置的临时名字（打完包应该自己消失）
*.old

# 美术源文件（Aseprite 工程和原始 GIF，由作者自己保管；
# 仓库只收 assets/ 下的派生帧图，可用 tmp/prepare_char_art.py 重新生成。
# 目录名后来被解压成了「人物(1)」等变体，用通配一起忽略）
人物*/
